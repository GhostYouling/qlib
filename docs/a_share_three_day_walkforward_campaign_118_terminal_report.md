# Campaign118 历史滚动终止报告

Campaign118 已完成唯一事前冻结的 2019–2023 三日滚动试验，开发 survivor 为 0，2024–2025 历史已暴露准样本外区间保持关闭且没有读取收益。

唯一 higher 因子 `intraday_top_decile_amount_event_spacing_entropy_25g` 从 09:31–11:30、13:01–15:00 的 240 根一分钟原始 `amount` 中，以“金额降序、精确并列时更早时点优先”选择固定 24 个事件；随后丢弃金额大小，对事件前、事件间、事件后的 25 个非事件间隔计算归一化 Shannon 熵。快照含 33,015 个分区、7,724,498 行，全部公式有效。PIT 质量/上市基准上的日覆盖中位/P05 为 `99.945175%/99.568865%`，P05 有效股票 `138` 只，可形成 `540` 个非重叠三会话 cohort，1,632 个会话均有横截面变化。

按冻结顺序完成的 135 项数值唯一性比较全部通过，最大绝对日截面秩相关中位数为 `0.629874`，对应 `intraday_above_median_amount_longest_run_240m`，低于严格 `<0.8` 门槛。由此只打开一次 higher 单因子开发试验，没有反向、事件数、并列规则、间隔定义、方向、阈值、年份子集、过滤、拟合、模型或组合搜索。

三组验证折的正 mean Rank-IC / normalized return / 10bp pilot return 折数为 `3/2/0`。验证中位 mean Rank IC 为 `0.047155`，验证中位 spread 为 `0.171028%`，验证中位 normalized return 为 `2.364079%`，但验证中位 10bp pilot return 为 `-2.006328%`。最差验证 normalized drawdown 为 `-25.634790%`，也低于冻结的 `-25%` 下限。

成本敏感性揭示了核心失败：全开发期 20万元整手 pilot 在 0bp 为 `+4.292526%`，到 5bp 已变为 `-0.729204%`，10bp 为 `-6.473970%`，20bp 为 `-15.886799%`。因此唯一试验同时失败于 10bp pilot 三折均非正、验证中位 pilot 非正、最差 normalized drawdown 和聚合 20bp 收益门，永久终止且禁止救援。

Campaign118 的 16 次尝试包含 1 次值前科学定义、14 次实现/快照/门禁阶段和 1 次收益读取开发试验；8 次基础设施失败均追加保留。累计历史研究尝试为 `914`，累计收益读取开发试验为 `304`。v107 将完整定义库/数值比较器更新为 `144/136`；追加只用于未来语义和数值去重，不恢复本因子的研究资格。

Candidate49 继续作为唯一前瞻候选，两本账各 0 条且哈希不变；当日数据源失败保持闭锁，没有同日重试或失败响应重请求。没有历史回填、第二前瞻候选、当前评分、选股、仓位、订单或投资建议。下一轮 Campaign119 只能从真正不同、值前冻结的新机制开始。
