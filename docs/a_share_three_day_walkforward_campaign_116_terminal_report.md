# Campaign116 覆盖率终止报告

Campaign116 已按事前冻结门槛在数值比较和历史收益之前终止。唯一 higher 因子为 `intraday_amount_conditioned_directional_persistence_spread_236p`；其 33,015 个分区、7,724,498 行、5,177,430 个公式有效值的快照已经再次通过全部分区字节/帧哈希及聚合摘要校验。

覆盖率审计使用冻结的 2019–2025 PIT 质量与当前上市资格基准，共 1,331,759 个资格股票日、1,632 个会话。结果如下：

- 中位日覆盖率为 `85.013381%`，低于 `95%` 门槛；
- P05 日覆盖率为 `71.417946%`，低于 `90%` 门槛；
- P05 有效名称数为 `121`，通过 `50` 门槛；
- 可形成 `539` 个非重叠三信号会话 cohort，覆盖 2019–2025 七年；
- `1,632` 个会话均有至少两个精确不同的有限横截面值，通过 100 会话的非恒定变化门。

门禁要求全部条件同时通过，因此两项覆盖率失败足以终止候选。134 个冻结比较因子读取数为 0；日线价格、forward return、2019–2023 开发折和 2024–2025 压力收益均未打开。这一结果只说明冻结的每组至少 30 个信息对支持规则造成可用性不足，不说明 higher 方向对三日收益为正或为负。

Campaign116 不得降低支持数、改变严格中位金额分组、反向、重设阈值/窗口/滞后、过滤、拟合、组合或以其他方式救援。完整语义定义库维持 142 项；数值比较器维持 134 项，Campaign116 不追加。有效会计仍为 5 次尝试、4 次基础设施失败、1 次值前科学尝试、1 个完整定义和 0 次收益读取开发试验；累计历史尝试 883、累计收益读取开发试验 302。

Candidate49 仍是唯一前瞻候选，信号与执行账本各 0 条且哈希未变。没有历史回填、第二前瞻候选、当前评分、选股、仓位、订单或投资建议。下一轮只能从真正不同、在任何值前冻结的 Campaign117 机制开始；离线研究无需等待 16:30 或新增日线。

终态后的首次技能文档追加补丁因上下文不匹配而在任何写入前被拒绝；科学结果与所有研究值未变化。v103 只把有效会计更新为 Campaign116 6 次尝试、5 次基础设施失败和累计历史尝试 884；142/134、累计收益读取开发试验 302、Candidate49 空账本及全部终止边界不变。

随后终态测试指针补丁引用 Black 格式化前的上下文，同样在写入前被拒绝。v104 再追加一次非科学失败：Campaign116 有效会计为 7 次尝试、6 次基础设施失败、累计历史尝试 885；覆盖结果、142/134、收益读取试验 302 和 Candidate49 均不变。
