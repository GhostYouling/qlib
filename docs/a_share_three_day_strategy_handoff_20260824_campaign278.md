# A 股三日短线研究交接：Campaign278

Campaign278 已完成并在公式、方向、来源行和数值之前终止。事前冻结的 7 条公共外部状态路线中，灾害恢复、食品卫生、贸易配额、碳配额、学术合作和稀缺监管容量 6 条与既有不可变终止机制重叠；企业慈善/社会救助虽经济独立，但缺少完整、合法、可审计的 2019–2023 点时来源合同。没有形成新因子定义、数值比较器或预测结论，2024–2025 未打开，定义/比较库保持 `162/143`。

本轮权威结果是 `docs/a_share_three_day_walkforward_campaign_278_prevalue_mechanism_frontier_result_20260824.json`，SHA-256 为 `7816ee4a3c26625995bc46a1dd3448e0de7ef5790395ec912bbace497fb0a738`。当前追加审计账本是 `data/experiments/short_horizon/historical_walkforward/campaign_278/research_attempt_ledger_v1.json`；有效会计为 16 次尝试：7 次值前科学尝试、9 次纯基础设施失败，累计历史尝试 2,843，收益读取开发试验保持 315。最终链尖与文件哈希由终态状态及验证记录锁定。

9 次基础设施失败全部发生在研究值读取之前：7 个无效 `jq` 对象投影在编译阶段失败，一次 Campaign167 前沿文件名猜错，一次批量绑定中 5 个历史前沿路径猜错。修正只使用跟踪文件名、受限元数据和精确文件哈希；无失败输出被当作通过证据或科学证据。

Candidate49 仍是唯一前瞻候选。2026-08-24 的 `stock_basic_invalid_ts_code` 保持失败关闭，不得同日重试或补请求；信号/执行账本继续为 0/0，禁止历史收益/信号/执行回填和 Candidate50。活动数据根仍是 `/Volumes/DIsk/Disk-Coding/qlib/data`；`.env` 只验证普通文件、0600、Git 忽略和一个非空 Token 赋值，秘密值或摘要未读取、打印、记录或暂存。

持续目标保持 `active`。下一轮 Campaign279 只能先冻结新的有限概念目录，再做零网络的针对性历史前沿检查；不得用当前快照、不完整覆盖、兄弟提供方、主体角色、辖区、字段、指标、窗口、阈值或方向救援 Campaign278。历史结果不得生成当前评分、选股、仓位、订单，也不构成投资建议。
