# Campaign116：现有本地数据路径已重新启动

Campaign115 已在 provider 值、候选值和收益读取前终止。Campaign116 不依赖 `limit_list_d` 或 5,000 积分，使用现有已接受的 2019–2025 Tushare 一分钟研究快照；当前只冻结值前规则，尚未读取 Campaign116 历史分区值。

有限六项概念库只保留一个候选：`intraday_amount_conditioned_directional_persistence_spread_236p`，方向为 higher。它在两个固定 120 分钟半场内形成 236 个相邻收益对，以全日 240 个成交额的普通中位数固定划分高成交额/普通成交额状态；零收益对不参与，两个状态各至少需要 30 个有效对。每个状态内把连续两次上涨记为 +1、连续两次下跌记为 -1、异号记为 0，最终分数为两个状态均值之差的一半，范围固定为 [-1,1]。

该机制与无条件符号连贯、上涨成交额占比、金额领先相关性和价格冲击不对称存在重叠风险，但不是直接组合旧因子值。它必须在覆盖通过后与全部 134 个冻结比较器逐一比较；任一绝对中位日度秩相关不严格低于 0.8、重叠不足或支持不足都将值前终止。

若通过无收益门禁，只允许一个权重 1.0 的 2019–2023 三折开发试验，不拟合、不反转、不搜索阈值、窗口、支持量、过滤、子集或模型。2024–2025 在冻结开发幸存者出现前保持关闭。当前累计历史尝试为 879，收益读取开发试验仍为 302。

下一步是仅做纯公式实现和合成语义测试并冻结代码指纹，然后才能构建一次无收益快照。Candidate49 仍是唯一前瞻候选，禁止历史回填；本结果不产生当前评分、选股、仓位、订单或投资建议。
