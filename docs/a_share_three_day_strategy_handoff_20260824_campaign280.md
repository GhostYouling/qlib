# A 股三日短线研究交接：Campaign280

Campaign280 已完成并在公式、方向、来源行和数值之前终止。事前冻结的 7 条公共外部状态路线中，无障碍、航天、种业、渔业、通信和生物多样性 6 条与既有不可变终止机制重叠；社区/原住民协商、同意与利益分享虽经济独立，但缺少完整、合法、可审计的 2019–2023 点时来源合同。没有形成新因子定义、数值比较器或预测结论，2024–2025 未打开，定义/比较库保持 `162/143`。

本轮权威结果是 `docs/a_share_three_day_walkforward_campaign_280_prevalue_mechanism_frontier_result_20260824.json`，SHA-256 为 `6f510ec31704667f76cc1ae43070a49808e6d2e795d223b4b005e292ada3fe3c`。当前追加审计账本是 `data/experiments/short_horizon/historical_walkforward/campaign_280/research_attempt_ledger_v1.json`，SHA-256 为 `208c38dadf45368451809b33eade6fde63c06207111145e7eaf065eb9a203a31`；有效会计为 8 次尝试：7 次值前科学尝试、1 次纯基础设施失败，累计历史尝试 2,859，收益读取开发试验保持 315，链尖为 `509c94be45e91b3bb98a52040ce70516abb1a5e9c1d07bee7956df4538d38d34`。

唯一基础设施失败发生在研究值读取之前：初始文件名库存命令使用 BSD awk 不支持的三参数 `match()`，且尾部 `sort` 在未启用 pipefail 的管道中掩盖了错误退出。修正命令只用 `rg` 对跟踪文件名和有界 JSON 元数据做同语义检查；失败输出未作为通过或科学证据。

Candidate49 仍是唯一前瞻候选。2026-08-24 的 `stock_basic_invalid_ts_code` 保持失败关闭，不得同日重试或补请求；禁止历史收益/信号/执行回填和 Candidate50。活动数据根仍是 `/Volumes/DIsk/Disk-Coding/qlib/data`；`.env` 只验证普通文件、0600、Git 忽略和一个非空 Token 赋值，秘密值或摘要未读取、打印、记录或暂存。

持续目标保持 `active`。下一轮 Campaign281 只能先冻结新的有限概念目录，再做零网络的针对性历史前沿检查；不得用当前快照、不完整覆盖、兄弟提供方、主体角色、辖区、字段、指标、窗口、阈值或方向救援 Campaign280。历史结果不得生成当前评分、选股、仓位、订单，也不构成投资建议。
