# Campaign086：分钟 K 线收盘位置序列持续性

Campaign086 继续采用双轨制：历史滚动研究是主要迭代引擎，不等待 16:30；Candidate49 仍是独立且唯一的前瞻确认层。本报告只记录历史研究证据，不构成选股、仓位、订单或投资建议。

唯一事前冻结的 higher 因子是 `intraday_intrabar_close_location_serial_persistence_238p`。对 09:31–11:30 与 13:01–15:00 的 240 根分钟 K 线，正区间 bar 定义 `x=(log(close)-log(low))/(log(high)-log(low))`。仅在各 120 根半场内部形成相邻状态对，午休不连接；零振幅 bar 没有状态，只删除直接相邻对且不桥接。至少保留 120 对、lag/lead 总体方差都严格为正后，计算总体 Pearson `rho`，最终分数为 `(1+rho)/2`。没有搜索方向、窗口、阈值、过滤、子集、模型或组合。

## 快照与覆盖结果

不可变候选快照 manifest / dataset SHA-256 为 `848ce713344f1ce805e3180344107d1d4bee5f8350bbcf52f8089e391fae3803` / `49cf11b5d69fa38a72f1a977975e290d4ed0bfd2205be6c50ba08dc64844b8e3`。7 个年度分区覆盖 1,331,759 个冻结质量与当前上市资格股票日，其中 1,193,690 行因子有效；全部分区通过字节、frame、键唯一性、范围与聚合哈希校验。

1,632 个交易会话的覆盖中位数为 `92.318613%`，低于冻结门槛 `95%`；P05 覆盖为 `80.755044%`，低于门槛 `90%`。P05 可用股票数为 `127.55`、可形成 538 个不重叠三会话 cohort、观察年份为 2019–2025，这三项容量门通过，但不能抵消两项覆盖失败。

因此无收益审计按预注册在读取任何 115 个比较因子值之前终止。日线价格、forward return、2019–2023 开发折与 2024–2025 历史暴露压力收益均未打开。Campaign086 不产生收益、IC、成本或回撤证据。

## 失败记录与边界

追加台账保留 9 次基础设施失败和 1 个完整因子尝试，累计历史研究尝试从 572 增至 582，累计读取开发收益的试验保持 285。失败包括预注册比较器顺序哈希抄录、数值方差稳定性、缺失 `PYTHONPATH`、空身份分区、运行时委托递归、身份格式编码和审计 lint；每次均失败关闭并以窄范围追加修复，不改公式、方向、支持、覆盖门或研究目录。

Campaign086 永久按“覆盖门失败”终止；不得降低门槛、删除低覆盖年份或会话、放宽 120 对支持、桥接零振幅或午休、反向、过滤、重标、调窗、重跑、救援、建模或与终止因子组合。该数值定义只可作为以后事前冻结的完整重复检测库成员。当前库更新为 118 个完整定义、116 个数值比较器。

Candidate49 信号/执行台账仍各 0 条，SHA-256 分别为 `5193f00d7f36da003f53cec387900da4c3d002299f5dfcfc1a95eaa199ed3a79` / `d57a3e61eac969e42cafa418ccd8c0a5ee65dcf69da145bb2c096c2a02a2ea4f`。Campaign086 没有 provider 请求、历史回填、第二前瞻候选、当前评分、选股、仓位或订单；历史结果继续受当前上市快照幸存者偏差限制。
