# Campaign151 相对市场成交活跃度时钟因子：公式冻结

Campaign151 在完整 `155/142` 定义/数值比较器库之后审查了六个有限值前概念，并选中唯一新机制 `intraday_relative_amount_share_clock_center_240m`（higher）。它不使用旧因子分数：每个固定分钟先用个股成交额除以剔除自身后的同分钟同行平均成交额，再把 240 个相对活跃度归一化为单位质量，最后计算 `x_i=i/239` 的时钟重心。

该量与两个最近定义不同：个股成交额重心不读取市场同行曲线；市场成交额轮廓同步因子比较两条归一化曲线的形状，而 Campaign151 定位逐点自有/同行比值的时间中心。若个股曲线在每分钟都只是同行曲线的固定倍数，Campaign151 精确等于 `0.5`，不受共同 U 形日内轮廓影响。

唯一公式、方向、240 分钟网格、至少 50 个剔除自身的完整同行、算术均值分母、零值、缺失、归一化、端点和 `[0,1]` 范围已冻结。7 项合成测试通过；首次 Black 检查在冻结前退出 1，经机械格式化后 Black、Ruff 和测试通过，该失败已单独写入追加台账且未被掩盖。

当前完整语义定义库为 `156`，顺序 SHA-256 为 `928e7eff198f44fa3112d7e8ff94571c1ad18b85a1ee50a38f3896baf555847c`；数值比较器仍为 `142`，Campaign151 尚无数值资格。本阶段没有读取历史分钟源值、同行基准值、候选/比较值、日线或 forward return，2024–2025 仍关闭；没有 provider 请求，也没有改变 Candidate49 两本空台账。

下一步只能先冻结并用合成数据验证来源绑定的完整轮廓同行基准/快照构建器，证明逐分钟剔除自身、同行数和均值语义，然后才可读取历史分钟值并先做覆盖门。Campaign151 保持 active，尚未授权 Campaign152，也不产生当前评分、选股、仓位或订单。

## 发布校验会计追加

新增发布测试的首次 Black 检查退出 1，链式 Ruff 与 pytest 因此前置失败没有运行。只机械格式化这份尚未绑定的测试后，三文件 Black/Ruff 通过，纯公式与发布测试合计 `14 passed`。该非零退出追加为 `campaign151_infrastructure_002`；有效会计更新为 8 次尝试、2 次基础设施失败，累计历史尝试 `1311`，科学定义、`156/142` 库和所有值前边界不变。
