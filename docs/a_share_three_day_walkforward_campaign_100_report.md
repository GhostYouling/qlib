# A 股三日短线历史滚动 Campaign100 终局报告

Campaign100 的唯一 higher 因子 `intraday_unchanged_close_range_absorption_share_238p` 在上午、下午各自 120 根标准分钟 bar 内形成 238 个相邻目的分钟。每对定义目的分钟振幅 `h_j=ln(high_j/low_j)`，并以原始 float64 收盘价严格相等定义 `z_j=1[close_j==close_{j-1}]`，返回 `sum(h_j*z_j)/sum(h_j)`。午休不桥接，零振幅保留；要求全部 240 根 HLC 有限、严格为正且有序，总目的分钟振幅严格为正。不使用 epsilon、价格容差、阈值、过滤、改窗、模型或组合。

不可变 7 分区快照含 1,331,759 行和 1,328,142 个有限值；manifest / dataset SHA-256 为 `31fa90e958d400aad250d369f4776f139664da869e9f152f8d4610e3351c20f4` / `b6a2ea043502b1c7c97726befffa0fe872d693a0cfa1f477fc2da8eab81d106d`。覆盖中位/P05 为 `99.835571%/99.337089%`，P05 合格名称 138，可形成 539 个不重叠三会话 cohort，覆盖门通过。

随后按冻结顺序完整读取 128 个数值比较器；125 项通过，3 项失败。与 `intraday_price_update_share_238m` 的绝对中位日秩相关为 `0.912352`，与 `intraday_terminal_nominal_share_price_affordability_rank_240m` 为 `0.894799`，与 `intraday_return_weak_order_entropy_234t` 为 `0.885712`，均超过严格 `0.8` 上限。最大相关的符号为负，但冻结门比较绝对值，因此不能通过反向绕过。

候选在数值去重门永久终止。审计未读取日线价格或 forward return，2019–2023 开发试验和 2024–2025 压力区间均未打开。最终记录 12 次历史研究尝试、13 条追加记录、4 次实现/基础设施失败、7 个值前概念/公式尝试和 1 个完整无收益因子结论；累计历史尝试 722，累计开发收益试验保持 294。

未来政策 v53 固定 132 个完整定义和 129 个数值比较器，并把 Campaign100 的高覆盖终止快照追加为负控制比较器。Campaign100 不得反向、残差化、删比较器、改相等定义、改窗、过滤、重跑、救援或组合。Candidate49 仍是唯一活动前瞻候选且两本台账各 0 条；本轮没有 provider 请求、历史回填、第二前瞻候选、当前评分、选股、仓位、订单或投资建议。

终态组合校验随后追加两次不读研究值的基础设施失败：第一次 Black 检查范围误包含已经由值前冻结和已发布快照绑定的特征脚本，因此只报告“would reformat”而未改写；第二次更新终态测试的多段补丁因 Black 已换行的断言上下文不匹配，在写入前被拒绝。修正后的当前节点只对无收益审计和终态文件执行静态检查，同时保留特征测试。最终口径更新为 14 次历史尝试、15 条记录、6 次实现/基础设施失败；累计历史尝试 724、累计开发收益试验仍为 294，科学结论和 132/129 顺序不变。
