# Campaign120 历史滚动终局

Campaign120 的唯一 higher 候选 `intraday_return_direction_dictionary_phrase_count_238s` 把上午和下午各 119 个相邻 close 比较编码为 `-1/0/+1`，每半日独立重置增量字典，按冻结的最短未见短语与终端后缀规则计数，再将两半计数相加。它不使用收益幅度、午休跃迁、成交量、成交额或日线字段。

不可变快照有 33,015 个分区、7,724,498 行，全部为 `[2,238]` 内整数。发布清单错误地保留了 `dictionary_parser_count=0` 和 `exact_symbol_support_required=false`；没有改写清单或重建分区，而是冻结只读恢复验证器，同时绑定错误原值与协议真实值 `1/true`，并验证全部分区字节、帧哈希、整数值和 dataset 摘要。

覆盖门通过：中位/P05 覆盖为 `99.945175%/99.568865%`，P05 有效股票数 `138`，有 540 个非重叠三会话 cohort，2019–2025 七年和 1,632 个非恒定横截面会话全部满足冻结门槛。

唯一性门终止：137 个比较器顺序完整，136 项通过；`intraday_range_weak_order_entropy_236t` 的绝对中位日 average-tie Spearman 为 `0.803679`，高于严格上限 `0.8`。失败比较覆盖 1,623 个会话，每日最少配对股票数 `52`，P05/P95 日相关为 `0.735967/0.871129`。与 Campaign119 的相关为 `0.650231`。

因此未创建 2019–2023 开发 trial，也没有读取日线、forward return 或 2024–2025 压力收益。科学终态形成时 Campaign120 有 17 次尝试；报告与终态测试维护的四项失败追加后最终为 21 次尝试、15 次基础设施失败，累计历史尝试为 `956`，累计收益读取开发 trial 保持 `305`。v113 将完整定义/数值比较器更新为 `146/138`，只用于未来重复检测，不允许对 Campaign120 做任何重解析、调参、反向、过滤、组合或收益重开。Campaign119+120 定向链 `36 passed`，最终权威终止测试 `6 passed`，Black/Ruff 通过。Candidate49 继续是唯一前瞻候选，两本账各 0 条。
