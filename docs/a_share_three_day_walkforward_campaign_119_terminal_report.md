# Campaign119 历史滚动终止报告

Campaign119 已完成唯一事前冻结的 2019–2023 三日滚动试验，开发 survivor 为 0，2024–2025 历史已暴露准样本外区间保持关闭且没有读取收益。

唯一 higher 因子 `intraday_range_boundary_direction_state_entropy_238p` 使用 09:31–11:30、13:01–15:00 的 240 根一分钟原始 high/low。它在上午和下午分别形成 119 个相邻对，排除午休跃迁，以 `(low 方向, high 方向)` 的精确九状态联合分布计算归一化 Shannon 熵；精确不变为 0，零宽 bar 有效。快照含 33,015 个分区、7,724,498 行，全部公式有效。PIT 质量/上市基准上的日覆盖中位/P05 为 `99.945175%/99.568865%`，P05 有效股票 `138` 只，可形成 `540` 个非重叠三会话 cohort，1,632 个会话均有横截面变化。

按冻结顺序完成的 136 项数值唯一性比较全部通过，最大绝对日截面秩相关中位数为 `0.683161`，对应 `intraday_range_weak_order_entropy_236t`，低于严格 `<0.8` 门槛。由此只打开一次 higher 单因子开发试验，没有反向、容差、状态合并、转移熵、窗口、阈值、年份子集、过滤、拟合、模型或组合搜索。

三组验证折的正 mean Rank-IC / normalized return / 10bp pilot return 折数均为 `1/1/1`。验证中位 mean Rank IC 为 `-0.000190`，验证中位 spread 为 `0.209527%`，验证中位 normalized return 为 `-3.768727%`，验证中位 10bp pilot return 为 `-2.119151%`。最差验证 normalized drawdown 为 `-30.703192%`，低于冻结的 `-25%` 下限。

全开发期 20 万元整手 pilot 在 0bp、5bp 和 10bp 分别为 `+15.287506%`、`+8.827153%` 和 `+3.195040%`，但 20bp 为 `-5.363866%`。正相关、normalized return 和 10bp pilot 都只出现在一折，且验证中位关联/收益非正；因此它没有跨年份稳定性，并同时失败于 20bp 成本和最差回撤门，永久终止且禁止救援。

Campaign119 的科学终态形成时有 20 次尝试：1 次值前科学定义、18 次实现/快照/门禁阶段和 1 次收益读取开发试验。终态审计又发现冻结记录的手工时间标签快于实际文件创建时间；冻结字节、激活顺序和研究值均未改变，追加式时间更正将该问题计为第 11 次基础设施失败。因此最终为 21 次尝试，累计历史研究尝试 `935`、累计收益读取开发试验 `305`。v109 保留 v108 的完整定义库/数值比较器 `145/137`；追加只用于未来语义和数值去重，不恢复本因子的研究资格。

Candidate49 继续作为唯一前瞻候选，两本账各 0 条且哈希不变；当日数据源失败保持闭锁，没有同日重试或失败响应重请求。没有历史回填、第二前瞻候选、当前评分、选股、仓位、订单或投资建议。下一轮 Campaign120 只能从真正不同、值前冻结的新机制开始。
