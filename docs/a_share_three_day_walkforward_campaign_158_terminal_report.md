# A 股三日短线 Campaign158 本地字段残余空间值前终止报告

记录时间：2026-08-15 04:18 Asia/Singapore

Campaign158 没有再列一组依赖外部缺失来源的概念，而是对本地 17 个事件/基本面 Parquet 做了逐字段 schema 审计。审计只调用 `pyarrow.parquet.read_schema`：覆盖 89 个有序字段出现、49 个唯一字段名，没有打开 row group、列值、候选值、比较值、价格或收益。

所有字段都得到明确处置。已经归一化的比例、增长、精度和事件强度字段，已在交易事件、公告事件、分析师、调研、所有权、融资和财务预测分支中直接测试或终止；事件数/覆盖数要么已直接测试，要么在原合同中仅为上下文。绝对金额或股数中，股东人数绝对变化、回购金额和质押股数已经测试；仅剩的解禁实际股数缺少事前冻结的尺度分母，此时再选择总股本、流通股、市场价值或利润分母会重组已终止来源。

`announcement_date`、`trade_date`、`event_date` 和 `report_date` 是可用时钟或报告期连接键；合法的 freshness、timeliness、delay consistency、peer crowding 和跨报告期变化均已有终止记录，不能直接排名日历序号。内部人数据没有实际历史披露时间，只有交易日和保守的第三交易日合成可用时间，不能把合成时钟伪装成实测披露延迟。证券代码、预测类型、schema 元数据、文件顺序和 margin smoke 副本也不是新的独立经济状态。

有限残余目录共 6 类，全部在读值前被拒绝，选中候选为 0；这表示“本地现有 schema 无合法未消费原料”，不表示来源中并不存在的实际披露延迟没有预测力。没有新增公式、方向、参数、过滤、组合、模型、定义或比较器。

一次基础设施失败已如实记录：系统 `/usr/bin/python3` 缺少 `pyarrow`，在导入阶段退出 1，未打开 schema、未产生部分输出；随后使用仓库既定的 Python 3.12 环境完成同一只读范围。Campaign158 因此计 6 次科学尝试和 1 次基础设施失败，累计历史研究尝试为 `1380`，收益读取开发试验仍为 `314`，完整定义/比较器保持 `157/142`。

Candidate49 仍是唯一前瞻候选，两本账均为 0 条；周六没有运行日更、读取凭据或请求 provider/web。目标保持 active。Campaign159 只能从真正新的原始状态或在任何行被观察前冻结的精确来源合同开始。
