# Campaign114：交易所信息披露评价的值前来源发现

Campaign114 没有继续重排分钟 OHLCV/amount 算子，也没有复活已终止的事件、报表或完整因子库模型。有限目录共审查 6 个构想，并对 v80 的 141 个完整定义做语义去重；只选中 `official_exchange_information_disclosure_evaluation_grade` 进入“元数据来源发现”，其余年报文本、当前董事会快照、财务字段扫参、一般公告计数和非线性完整库模型均在读值前拒绝。

选中只说明机制语义可能独立，不说明数据可用或收益有效。候选意图使用沪深交易所按年发布的发行人信息披露工作评价等级，方向仅预声明为“官方等级越好，分数越高”。目前没有证明两交易所都存在 2019–2025 稳定档案，也没有冻结等级词表、数值映射、可用时点、窗口、缺失规则、适配器或来源合同，因此它还不是完整因子定义。

来源发现协议已固定为双交易所、三个精确检索词和元数据只读。只允许记录官方静态搜索/导航/索引页面的 URL、目录标识、可见标签、发布日期标签、分页元数据和未打开的附件 href；禁止打开或下载 PDF/XLS/XLSX/ZIP，禁止读取、统计或推断公司代码、名称、年份或等级行，也禁止第三方补全、OCR、手工名称匹配、删掉一个交易所或曝光后扩词。即使元数据门通过，也只能进入下一轮的等级映射、纯合成适配器、公式/时点和 source-contract 冻结，仍不能直接打开名单。

本轮两类预协议搜索失败均被保留：联网搜索连接器没有返回结构化结果，公共搜索 HTML 只返回查询壳而没有可操作的交易所链接。另有一次本地 `python` 别名不存在的链哈希工具失败；改用 `python3` 后仅计算固定字符串哈希，没有接触研究值。Campaign114 当前共 4 次尝试，其中 3 次基础设施失败、1 次值前科学尝试、0 个完整因子、0 次收益读取试验；累计历史尝试为 849，累计收益读取开发试验保持 302。

v81 原样保留 141 个完整定义和 134 个数值比较器；没有来源行、附件、候选值、比较值、日线、forward return、2019–2023 开发或 2024–2025 压力读取。Candidate49 仍是唯一前瞻候选且信号/执行台账各 0 条；周日未运行 plan/run，没有历史回填、第二前瞻候选、当前评分、选股、仓位、订单或投资建议。

## 验证会计追加

首轮定向测试的库顺序断言误用链式等式，把右侧哈希比较先求成布尔值，得到 `23 passed, 1 failed`；底层 v80/v81 哈希实际一致。该测试实现失败已追加到 v2 台账，修复仅拆成两条独立断言，没有修改研究定义或读取研究值。有效政策顺延为 v82，仍保持 141/134；Campaign114 现为 5 次尝试、4 次基础设施失败，累计历史尝试 850，累计收益读取开发试验仍为 302。

## Campaign114 官方元数据终止

协议绑定执行使用且仅使用三个冻结检索词。上交所官方索引页和年度结果页确认了若干年度页面、发布日期、附件标签以及官方规则中的 A/B/C/D 等级词表；深交所官方新闻和规则元数据也确认了同一等级词表。但在完整证明两交易所 2019–2025 档案及全部点时角色之前，深交所静态文档域的精确检索响应直接展开了一个 2024–2025 评价附件中的公司代码、简称和等级行。

本轮没有点击、打开、下载、截图或哈希附件字节，也没有把任何具体公司等级持久化或计数；然而搜索响应本身已经越过“映射、公式、纯适配器和 source contract 先冻结”的值前边界。按冻结协议，Campaign114 必须在完整定义之前终止，禁止改搜索引擎、换浏览器、补标签、删交易所、改来源或事后冻结映射。

最终 Campaign114 为 7 次尝试：5 次基础设施失败、2 次值前科学尝试、0 个完整定义、0 个数值快照和 0 次收益读取开发试验。累计历史尝试为 852，累计收益读取开发试验保持 302。v83 原样保留 141 个完整定义和 134 个数值比较器；没有候选/比较值、日线、forward return 或 2024–2025 压力读取。Candidate49 仍是唯一前瞻候选且两本账为空；周日没有 plan/run、历史回填、当前评分、选股、仓位、订单或投资建议。

首次跨 Campaign112–114 终态套件得到 `31 passed, 1 failed`：本报告标题缺少 `Campaign114` 前缀，统一标记断言未命中。修复只补全标题并追加验证会计，没有修改科学结论或读取任何研究值。最终有效会计为 8 次尝试、6 次基础设施失败、2 次值前科学尝试，累计历史尝试 853、累计收益读取开发试验 302；v84 仍保持 141/134。
