# A 股三日历史滚动研究：Campaign127 终态报告

记录日期：2026-08-14（Asia/Singapore）

## 结论

Campaign127 在来源结构/规范化验收阶段终止。该结论不是三日收益失败，也不能解释为因子有效或无效：候选值、139 项比较值、日线、forward return、2019–2023 开发收益和 2024–2025 压力值均未打开。

有限值前目录共六项，只选择一个真正不同且不依赖 5,000 积分数据的定义 `convertible_issue_online_demand_180d`。它只使用 `cb_basic` 的 `ts_code,stk_code,cb_type` 和 `cb_issue` 的 `ts_code,ann_date,res_ann_date,onl_pch_excess`；官方文档对两个接口均标注最低 2,000 积分。方向固定为 higher，公式为：对结果公告日后首个接受的本地交易会话起、年龄 1–180 个自然日内的唯一最新事件，返回 `log1p(onl_pch_excess)`，否则缺失。事件年龄不参与权重或并列处理，同一股票同一最新结果日存在多只可转债时直接失败。

发行规模、稀释、发行价格、成本、评级、可转债市场价格及替代接口均在值前排除；本轮没有组合旧终止因子，也没有为失败来源准备救援字段、窗口、方向或模型。

## 验证与来源终态

纯适配器与公式通过 17 个合成测试；一次性验收工作流另通过 9 个合成测试，合计 26 个测试，Black 与 Ruff 均通过。精确 `plan` 以退出码 0 返回 `ready=true`，29 项检查全部通过且未请求供应商。

唯一确认运行加载 `.env` 中的凭据但未打印、哈希或持久化秘密，共发出 9 次请求：1 次 `cb_basic` 和 2018–2025 八次年度 `cb_issue`。运行退出码为 2，唯一持久化的脱敏失败码是 `source_schema_or_canonicalization_contract_failure`。没有重试，没有为补充细节重新请求响应，也没有保存异常内容、原始行或行数；接受快照与接受 manifest 均未创建。

完整逻辑定义库由 149 增至 150，顺序哈希为 `c3713559f968c7f79b070398742ef625733e9c178d4e0c0010805fa12c5d5142`。由于来源未通过，数值比较器仍为 139，顺序哈希保持 `9c4de054ca1b83eced67eece256b024ebec3a6e1925de17a4cedfa7d36265161`。新定义只保留给未来语义去重，不得重试或进入数值比较。

## 台账与边界

Campaign127 最终记录 8 次尝试：4 次基础设施失败、4 次值前科学尝试、1 个完整逻辑定义、1 次来源验收、0 次收益读取开发试验。累计历史尝试为 1,033，累计收益读取开发试验保持 306。所有失败均追加保留，没有只记录最佳结果。

Candidate49 继续作为唯一前瞻候选，信号与执行台账均为 0 条；没有历史回填、第二前瞻候选、当前评分、选股、仓位、订单或投资建议。Campaign128 只能从真正新的值前机制，或另行事前验收通过的点时来源开始。
