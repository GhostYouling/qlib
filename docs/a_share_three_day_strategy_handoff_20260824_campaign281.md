# A 股三日短线研究交接：Campaign281

Campaign281 已完成并在公式、方向、来源行和数值之前终止。事前冻结的 7 条公共外部状态路线全部与既有不可变终止机制重叠；没有形成经济独立且来源合同就绪的新路线，没有新因子定义、数值比较器或预测结论，2024–2025 未打开，定义/比较库保持 `162/143`。

本轮权威结果是 `docs/a_share_three_day_walkforward_campaign_281_prevalue_mechanism_frontier_result_20260824.json`，SHA-256 为 `92707d64b861dbd16ee410ec206d5bc3e2a5c387823779ad0f5f8b396bd0e85b`。有效追加审计账本是 `data/experiments/short_horizon/historical_walkforward/campaign_281/research_attempt_ledger_v4.json`，SHA-256 为 `4a0f9ff0aa8eb3038d96b84a3b5acb58e4fc781a5a1a9b72f1596d266d49be9a`；有效会计为 10 次尝试：7 次值前科学尝试、3 次纯基础设施失败，累计历史尝试 2,874，收益读取开发试验保持 315，链尖为 `19e8be2f603309fd84612d78eb59e65a282856e0e618cfaf8d4692537bb38cf8`。

三次基础设施失败均发生在研究值读取之外。第一次是只读检查把 Campaign280 验证记录文件名的 `v2` 与日期顺序写反；第二次是新建的 Campaign281 v2 台账条目哈希不符合冻结算法；第三次是状态路径补丁后 Black 要求机械格式化。无效 v2 保留为失败证据但从未被权威绑定；有效 v3 从合法 v1 重建前两条失败，v4 追加格式失败并通过独立链校验。失败输出未作为科学证据。

Candidate49 仍是唯一前瞻候选。2026-08-24 的 `stock_basic_invalid_ts_code` 保持失败关闭，不得同日重试或补请求；禁止历史收益/信号/执行回填和 Candidate50。活动数据根仍是 `/Volumes/DIsk/Disk-Coding/qlib/data`；`.env` 只验证普通文件、0600、Git 忽略和一个非空 Token 赋值，秘密值或摘要未读取、打印、记录或暂存。

持续目标保持 `active`。下一轮 Campaign282 只能先冻结新的有限概念目录，再做零网络的针对性历史前沿检查；不得用当前快照、不完整覆盖、兄弟提供方、主体角色、辖区、字段、指标、窗口、阈值或方向救援 Campaign281 或更早路线。历史结果不得生成当前评分、选股、仓位、订单，也不构成投资建议。
