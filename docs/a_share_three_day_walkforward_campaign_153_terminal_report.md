# A 股三日短线 Campaign153 来源状态前沿值前终止报告

记录时间：2026-08-15 02:40 Asia/Singapore

Campaign153 已在读取来源行、候选值、比较器值、历史价格或前瞻收益之前终止。本轮从完整 157 定义与 142 个数值比较器之外审查 6 个有限机制：相对报价价差、主动买卖成交额失衡、平均单笔成交额、撤单后的队列补单、成交额/流通市值和日线推断涨停队列持续性。

前四项确实是与现有 OHLCV/amount 因子不同的微观结构状态，但已接纳的本地历史合同只有 bar 级 OHLCV、成交额和日线价格字段，没有买一/卖一、主动方、逐笔笔数/标识或订单新增/撤销/队列数量，因此不能实现。用分钟振幅、涨跌符号、成交量或吸收率做代理只会把已终止因子换名字。

后两项也未通过：历史点时 `circ_mv`/股本不在接纳合同中，当前上市快照不能倒灌，而且成交额/流通市值与已暴露换手率同族；Campaign115 的官方涨停队列机制已因继续研究范围不包含所需授权源而在取值前终止，日线 OHLC 既缺少点时板块/ST 涨跌停规则，也无法恢复封板时间、开板次数或排队金额。

因此没有创建新公式、快照、适配器或模型，没有运行来源验收、覆盖率、数值唯一性、2019–2023 收益检验或 2024–2025 准样本外压力阶段。完整库保持 157 个定义，数值比较器保持 142 个。Campaign153 追加 6 次科学概念尝试、0 次基础设施失败；累计历史研究尝试为 1,340，累计读取收益的开发试验仍为 314。

Candidate49 仍是唯一前瞻候选，两本账均为 0 条且指纹未变。今天是周六，没有运行日更 plan/run，没有读取凭据、请求 provider、历史回填、当前评分、选股、仓位或订单。本结果仅用于研究流程，不构成投资建议。

持续迭代目标保持 active。下一轮 Campaign154 只能从新的有限机制和已经接纳的点时历史来源开始，或先建立一条独立、明确授权的 source-admission campaign；不得重建 Campaign153 的代理变量或重开 Campaign138、Campaign115、Campaign152 的终止分支。

## 终态验证会计追加

首次聚焦验证中，新增测试误从完整日线快照的上层 manifest 直接读取 `fields`，实际字段清单由其中的 `acceptance_manifest_path` 和 `acceptance_manifest_sha256` 绑定，因而 1 项失败、8 项通过。修复只让测试先验证并读取该 acceptance manifest；没有改变任何科学定义、来源合同或门槛。修复后 Black、Ruff 均通过，聚焦测试 `9 passed`。非零退出未被掩盖，作为 1 次基础设施失败追加；Campaign153 最终有效尝试为 7 次，累计历史研究尝试为 1,341，收益读取开发试验仍为 314，科学终态与 `157/142` 库不变。

## 回归验证会计追加

Campaign145–153 回归首次从 `tests/` 工作目录运行，仓库根不在模块路径，7 个测试文件在收集阶段报 `No module named scripts`；回到仓库根后，第一次排除参数又错误使用了带 `tests/` 前缀的调用路径，已知的 Campaign145 可变报告生命周期节点仍被选中，结果为 63 项通过、1 项失败。两次非零退出均保留。使用 pytest 实际收集的精确 node id 后，最终回归为 `63 passed, 1 deselected`。Campaign153 最终有效尝试为 9 次，其中 6 个科学概念和 3 个基础设施失败；累计历史研究尝试为 1,343，收益读取开发试验仍为 314，科学终态、`157/142` 库和 Candidate49 边界均不变。
