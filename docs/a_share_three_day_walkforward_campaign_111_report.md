# A 股三日短线 Campaign111：事前机制前沿零候选终止

Campaign111 在读取任何新源行、候选值、比较值、日线价格或未来收益前终止。停止原因不是历史样本不足，而是有限候选库中的 6 个构想都没有通过“真正独立机制”门禁。

本轮按 v72 的冻结顺序语义复核全部 140 个完整定义，并为可能通过的候选预留全部 134 个数值比较器，但没有加载比较值。收益符号净占比会改变已终止 sign-run 因子的符号/零值/顺序处理；running-drawdown ulcer 指标会改写已终止 drawdown 与 frontier 家族；Amihud 式冲击比已在旧协议中值前拒绝；成交均价在 high-low 区间的位置重试 Campaign013 的已知支持失败且 Campaign096 已值前拒绝其加权变体；开盘参考 crossing 是 Campaign110 与 Candidate49 crossing 运算的重表达；134 因子单纯形重拟合则重试 Campaign102 的完整库模型。

因此没有冻结数值因子、没有特征快照、没有覆盖或唯一性数值审计，也没有运行 2019–2023 开发折或打开 2024–2025。v73 原样保留 v72 的 140 个完整定义与 134 个数值比较器，顺序摘要分别为 `c06e054b...` 与 `31d788db...`。

追加式台账共 3 条：2 次只读路径假设失败和 1 次完整的事前科学审计；所有失败与 6 个构想均保留。累计历史研究尝试从 822 增至 825，累计读取开发收益的试验保持 302。

Candidate49 仍是唯一活动前瞻候选，信号与执行台账各 0 条且 SHA-256 未变。当前是周日，本轮没有运行 Candidate49 plan/run，也没有 provider 请求、历史回填、Candidate50、当前评分、选股、仓位、订单或投资建议。`.env` 仅核验为 Git 忽略的普通 0600 文件且唯一 token 条目非空；秘密未打印、哈希、记录或持久化。

下一轮 Campaign112 只能从一个真正独立且值前冻结的机制开始，或先验收能识别新经济状态的新数据源语义。Campaign111 的六个构想不得改名、改窗口、改支持、换权重或组合后重试。
