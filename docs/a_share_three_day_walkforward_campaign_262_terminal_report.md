# Campaign262 terminal report

Campaign262 tested one pre-value-frozen factor, `intraday_amount_schedule_uniformity_240m` (higher). For the exact 240 continuous-session minute amounts, it compares the cumulative normalized amount path with the fixed uniform clock path and returns one minus the cumulative-L1 distance normalized by its exact maximum 119.5. The score has exact range [0, 1] and uses no price, return, peer benchmark, threshold, fit, model or combination.

The immutable snapshot contains 33,015 partitions and 7,724,498 eligible rows. On the frozen quality/current-listing base, candidate coverage passed: 1,330,171 eligible rows, median/P05 daily coverage 0.999452/0.995689, P05 eligible names 138, 540 non-overlapping three-session cohorts, and 1,632 nonconstant cross-sections.

All 142 numeric comparisons were recorded. One failed the strict absolute median daily average-tie Spearman threshold below 0.8:

- `intraday_amount_center_of_mass_240m`: +0.890690 across 1,623 sessions.

Campaign262 therefore terminated before every daily price, forward return, 2019–2023 development fold, and 2024–2025 stress return. No inversion, transform, subwindow, filter, residualization, model, combination or rescue is allowed. The definition is appended once to duplicate control, but it is not appended to the eligible numeric comparator set.

Fourteen infrastructure or verification failures and all seven finite scientific routes are preserved in the append-only ledger; no failed or truncated output is used as scientific or validation evidence. The first terminal result transcribed an invalid shortened predecessor hash and is preserved as failed lifecycle evidence; terminal result v2 binds the independently measured hash. Effective Campaign262 accounting is 21 attempts, bringing cumulative historical attempts to 2,559 while cumulative return-reading development trials remain 314.

Candidate49 remains the sole prospective candidate with unchanged empty signal and execution ledgers. Sunday required no Candidate49 daily plan. No credential presence, value or digest was read; no provider or web request was issued. This historical result does not authorize current scores, selections, sizing, positions, orders, or investment advice. After terminal validation, Campaign263 may begin a new finite zero-network prevalue mechanism at any local time.

## Recovery-chain accounting correction

The first focused terminal run passed 33 tests and failed one chain test because ledger v1 declared 20 entries and a post-entry chain tip but its `entries` array was empty. The generator had computed each hash but omitted the append operation. Ledger v1 and its dependent v2 remain immutable failed lifecycle evidence. Recovery ledger v3 reconstructs all 22 attempts directly from the Campaign261 chain tip, including the prior hash-binding failure and this failed focused run. Effective Campaign262 accounting is therefore 22 attempts: 7 scientific routes and 15 infrastructure failures. Cumulative historical attempts are 2,560; return-reading development trials remain 314. The scientific result, 161/142 library state, Candidate49 0/0 ledgers and all no-return/current-use boundaries are unchanged.

## Final validation accounting correction

Two base-catalog inspection commands guessed the wrong v311 date path and exited nonzero; a first exact cumulative run then completed after the outer orchestration yielded, but its nested session ID and output were not preserved. All three outputs were discarded and recorded. The independently rerun frozen Campaign145–262 boundary contains 164 test files and passed with 628 passed, 1 skipped and 75 exact deselections. Effective Campaign262 accounting is 25 attempts: 7 scientific routes and 18 infrastructure failures. Cumulative historical attempts are 2,563; return-reading development trials remain 314.

## Terminal validation passed

The current recovery chain, terminal result v4, numeric policy v407, 161/142 library semantics, Candidate49 empty-ledger semantics and research boundaries pass the 34-test Campaign262 focused scope. Black and Ruff pass for all five Campaign262 test files. The final cumulative rerun remains 628 passed, 1 skipped and 75 deselected across 164 files. Campaign263 is authorized only for a new finite, independent, zero-network offline prevalue mechanism.
