# Campaign110 历史滚动研究终局

唯一 higher 因子 `intraday_open_reference_directional_occupancy_240m` 以 09:31 原始 open 为固定参考，在精确 240 根连续竞价分钟（09:31–11:30、13:01–15:00）上计算 `mean(sign(log(close_i/open_09_31)))`。每个 close 和锚点必须有限且严格为正；精确等于参考价的 bar 保留为 0，分母始终为 240。没有搜索反向、锚点、窗口、容差、阈值、过滤、子集、模型、权重或组合。

不可变快照有 33,015 个分区、7,724,498 行且全部有效；覆盖中位/P05 为 `99.945175%/99.568865%`，P05 合格名称 138，可形成 540 个不重叠三会话 cohort。按 v70 冻结顺序的 133 项数值比较全部通过，最大绝对中位日截面秩相关为 `0.719726`，低于严格 `<0.8` 门槛，因此只打开唯一的 2019–2023 三折开发试验。

开发门最终失败：正 mean Rank-IC 折数 `1/3`，正 normalized-return 折数 `1/3`，正 10bp pilot-return 折数 `0/3`；验证中位 mean Rank IC `-0.005767`，验证中位 normalized return `-3.438009%`，验证中位 10bp pilot return `-2.140528%`，最差验证 normalized drawdown `-29.663535%`，2019–2023 聚合 20bp pilot return `-19.211693%`。操作门通过，但验证质量门失败，survivor 为 0。

2024–2025 stress 未打开、未读取；记录状态为 `not_opened_zero_development_survivors`。Campaign110 不得反向、换锚、改窗口/变换/相等语义、过滤、残差化、重跑、救援或组合。由于覆盖和全部唯一性门通过，v71 把本定义追加到未来重复控制库，形成 140 个完整定义和 134 个数值比较器。

首次统一报告多文件补丁因上下文不匹配失败且未修改任何文件，已作为第 12 次基础设施失败保留；恢复后逐文件追加。本轮最终共 13 次尝试：1 次完整科学尝试、12 次实现/验证/记录基础设施失败；读取收益的开发试验为 1 条。累计历史研究尝试 822，累计收益读取开发试验 302。Candidate49 的信号与执行账本仍各为 0 条；本轮没有 provider 请求、历史回填、第二个前瞻候选、当前评分、选股、仓位、订单或投资建议。本结果是历史研究，不是投资建议。
