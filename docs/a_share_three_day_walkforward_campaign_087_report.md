# Campaign087：日内振幅—成交额轮廓对齐度

Campaign087 继续采用双轨制：历史 walk-forward 是主要迭代引擎，不等待 16:30；Candidate49 仍是独立且唯一的前瞻确认层。本报告仅记录历史研究证据，不构成选股、仓位、订单或投资建议。

唯一事前冻结的 higher 因子是 `intraday_range_amount_profile_alignment_js_240m`。在精确 09:31–11:30 与 13:01–15:00 的 240 根 bar 上，定义每分钟对数振幅 `r_i=ln(high_i/low_i)`，将振幅和成交额分别归一化为概率轮廓 `p_i`、`q_i`，再返回 `1-JS(p,q)/ln(2)`。精确零振幅或零成交额保留为零质量；总振幅与总成交额必须严格为正。没有伪计数、平滑、删格、阈值、过滤、反向、模型或组合。

## 无收益门禁

不可变 v5 紧凑快照包含 7 个年度分区、1,331,759 个冻结质量/当前上市资格股票日，其中 1,328,449 个因子值有效；manifest / dataset SHA-256 为 `3a44a828a5c95712471e2757fc24225607759860f236f10de674fc93927b7bfe` / `9f92cede97522203ace1dc3879a862cc55e58e4877f91fc407f275d418ba6428`。覆盖中位/P05 为 `99.853694%/99.354839%`，P05 可用股票数 138，可形成 539 个不重叠三会话 cohort。

116 项冻结数值比较全部通过；最大绝对中位日秩相关为 `0.787143`，对应 Campaign053 的 `intraday_amount_price_discovery_alignment_js_238p`，严格低于 `0.8`，但距离门槛仅 `0.012857`，因此只说明定义未被重复检测拒绝，不代表收益稳健。

## 2019–2023 开发结果

唯一开发试验固定使用三组扩展折、边界清除 3 个信号会话，并要求 t+1 与 t+3 留在同一分区。2021/2022/2023 三个验证折的 mean Rank IC 为 `-0.035740/-0.025508/-0.052188`，normalized return 为 `+16.660467%/+47.546682%/-5.195431%`，10bp 整手收益为 `+0.961899%/+4.541692%/-2.178972%`。三个 IC 全负；中位验证 spread 为 `-0.036604%`，最差验证归一化回撤为 `-25.217549%`，略差于冻结的 `-25%` 门槛。

全开发期 375 个 cohort 的 normalized return 为 `+283.252839%`，10bp/20bp 整手收益为 `+13.052135%/+2.528237%`。这些聚合正收益不能覆盖三折 IC 一致性、验证 spread 和最差回撤失败。运营门通过，质量门失败，survivor 为 0；2024–2025 历史暴露压力区间没有打开，也没有读取收益。

## 终止与边界

追加台账最终保留 19 个历史研究尝试：1 个完整因子尝试以及 18 个基础设施、实现、验证、发布和元数据失败；另有 1 条同一因子的收益读取延续。累计历史研究尝试为 601，累计读取开发收益的试验为 286。Black 排版失败、快照发布元数据、紧凑比较器加载、终态生命周期断言、首次错误 deselect 前缀和手填时间戳超前均完整保留，且未改变公式、方向、覆盖门、比较顺序、折、成本或 survivor 门槛。文件系统出生时间证明预注册、实现冻结和激活绑定均早于收益读取；精确排除 5 条不可变前态断言后的当前终态套件为 `36 passed, 5 deselected`，Black/Ruff 通过。

Campaign087 永久按“0 survivor”终止；不得反向、改窗、平滑、加伪计数、过滤、重标、调参、重跑、救援或与终止因子组合。其数值定义只可作为下一轮事前冻结重复检测库成员。Candidate49 信号/执行台账仍各 0 条；本轮没有 provider 请求、历史回填、第二前瞻候选、当前评分、选股、仓位或订单。历史结果继续受当前上市快照幸存者偏差限制。
