# Campaign123 纯证券改名因子终止报告

## 结论

`official_pure_security_rename_recency_60s` 在数据源模式/规范化验收阶段终止。它没有进入历史因子值、数值去重、2019–2023 开发试验或 2024–2025 压力期，因此本轮没有关于三日收益方向或强弱的证据。

## 一次性验收

冻结的 `plan` 以退出码 0 返回 `ready=true`，全部 28 个检查通过。随后唯一一次确认运行按 2019–2025 七个年度窗口各发出一次 `namechange` 请求，最终退出码 2，脱敏失败码为 `source_schema_or_canonicalization_contract_failure`。

工作流没有重试，没有重新请求失败响应补细节，没有保存异常消息、token、原始行或行数，也没有生成规范事件快照和验收 manifest。intent 与 failure record 均为 0600；Candidate49 两本账未改变。

按照值前冻结规则，不能在看到失败后增加字段、读取 `name`、放宽日期或代码、扩展原因、改变 60 会话窗口/方向/缺失规则/门槛、换接口或重试救援。该逻辑定义留在 148 项完整库中只用于未来重复检测，139 项数值比较器不变。

Campaign123 最终 15 次尝试、11 次基础设施失败、4 次值前科学尝试、1 次来源验收、0 次收益读取开发试验；累计历史尝试 1014，累计收益读取开发试验 306。Campaign124 只能从真正新的值前机制或另行事前验收的数据源开始。

本报告是研究审计记录，不构成选股、仓位、订单或投资建议。
