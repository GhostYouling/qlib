# A 股三日短线历史滚动 Campaign101 终局报告

Campaign101 的唯一 higher 因子 `full_numeric_library_directional_lower_quartile_consensus_129f` 使用 v54 完整数值库的 129 个因子。在每个信号日截面内按各自冻结方向做 average-tie 百分位排名；组件缺失只在聚合器内部映射为 0，至少要求 97 个有限组件，将全部 129 个分数排序并返回零基第 32 项（即一基第 33 项）。没有搜索替代分位数、阈值、权重、子集、过滤、模型或方向。

不可变快照含 1,331,759 行、1,327,637 个有限值和 1,632 个会话；manifest / dataset SHA-256 为 `7a2db929fff69f46edbc93d57cdca9cc4427c47dd542291b4f58d879dd91a3b6` / `ed024ab2736b9766e1adb122b155189029f30a5d14e11d4d67a403ff9befef24`。中位/P05 覆盖为 `99.812383%/99.206641%`，P05 合格名称 138，可形成 539 个不重叠三会话 cohort。129 项冻结数值比较全部通过，最大绝对中位日秩相关为 `0.557897`，对象是旧完整库百分位共识因子。

唯一 2019–2023 开发试验在 2021/2022/2023 的 mean Rank IC 为 `-0.024095/-0.010092/-0.037910`，normalized return 为 `-22.509635%/-17.176792%/-6.828093%`，10bp 整手收益为 `-4.435141%/-3.488183%/-2.464085%`。三折 IC、normalized return 和 10bp 收益的正折数均为 0；中位 IC `-0.024095`，中位 10bp 收益 `-3.488183%`，最差验证 normalized 回撤 `-35.020163%`，全开发期 20bp 收益 `-21.717077%`。运营门通过但质量门失败，survivor 为 0。

因此 2024–2025 准样本外压力区间按事前规则未打开、未读取，也没有压力试验。首次开发启动在错误 Python 下缺少 `libc++.1.dylib`；项目 Python 恢复后又暴露动态继承加载器仍保留上一代 `1,328,449` 合格行常量，而冻结清单与外层包装器均为 `1,327,637`。v2 只同步这一实际加载函数常量，未改变公式、数据字节、折、成本或门槛。两次失败都保留在内部台账；继承生成的简报把独立 failure phase 误汇总为 0，权威口径以内部台账的 2 条为准，原始产物不改写。

最终有效口径为 19 次历史研究尝试、19 条追加记录、12 次实现/基础设施/记录失败、1 个完整无收益因子结论和 1 条收益读取开发试验；累计历史尝试 743、累计开发收益试验 295。未来政策 v55 固定 133 个完整定义和 130 个数值比较器，并只把 Campaign101 高覆盖终止快照作为负控制比较器追加。Campaign101 不得反向、残差化、改分位数、改缺失规则、删组件、改权重、过滤、重跑、救援或同轮重组。

Candidate49 仍是唯一活动前瞻候选，两本账各 0 条且哈希不变；本轮没有 provider 请求、历史回填、Candidate50、当前评分、选股、仓位、订单或投资建议。历史离线研究可继续，不需要等待 16:30 或新日线；下一轮只能在 v55 完整库和全新值前机制冻结后开始。

终态通用绑定扫描随后发现两份新记录中的 survivor SHA-256 字面量各缺最后一个十六进制字符；真实 survivor 文件及全部科学结果未变，测试因 fail-fast 尚未执行。错误记录原样保留，v2 只追加正确的 64 位绑定。最终有效口径更新为 20 次尝试、20 条记录、13 次实现/基础设施/记录失败，累计历史尝试 744；累计开发收益试验仍为 295。有效未来政策为 v56，133/130 顺序不变。

随后组合套件误纳入已消费的 recovery-v2 激活前置测试，得到 `38 passed, 3 failed`；三条失败都要求成功运行前的“两条失败、无 survivor、无 stress 记录”旧状态。冻结测试不改写，当前态套件明确排除两代已消费恢复测试。最终口径为 21 次尝试、21 条记录、14 次实现/基础设施/记录失败，累计历史尝试 745、累计开发收益试验 295；有效政策 v57 仍保持 133/130 顺序。

第二次组合又误纳入绑定追加前报告字节的 terminal-v2 中间测试，得到 `37 passed, 1 failed`。中间测试仍原样保留，稳定当前态白名单只含特征、无收益、科学开发、原始科学终态和最新有效终态测试。最终口径为 22 次尝试、22 条记录、15 次实现/基础设施/记录失败，累计历史尝试 746、累计开发收益试验 295；有效政策 v58 保持 133/130 顺序。
