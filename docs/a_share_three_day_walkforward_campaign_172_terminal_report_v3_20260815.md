# Campaign172 企业保险与风险转移来源前沿值前终止（累计回归节点恢复会计）

Campaign172 固定审查 7 条路线，科学结论仍为 4 条来源准入延期与 3 条值前重命名/算子拒绝；没有完整因子、比较器、收益试验或 2024–2025 开放。

首轮 Campaign145–172 累计回归将精确 deselect 节点写成 `tests/data_collector_tests/...`，但本仓库 pytest 根把节点规范为 `data_collector_tests/...`。因此 5 个已知的可变报告生命周期断言没有被 deselect，结果为 `374 passed, 1 skipped, 5 failed`；失败均是预期的历史发布哈希变化，Campaign172 聚焦测试此前已 `5 passed`。旧政策和旧测试字节未修改，失败已追加保留。

Campaign172 当前有效会计为 9 次尝试（7 科学、2 基础设施），累计历史尝试 `1518`，收益读取开发试验仍为 `314`；定义/比较器保持 `158/142`。Candidate49 空账本未变，持续目标保持 active。下一步只以 v254 修正精确节点 ID 后重跑。
