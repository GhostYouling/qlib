# A 股三日历史滚动 Campaign264 终端报告

Campaign264 已在任何候选值、比较值、日线价格或收益读取之前终止。结果不是“找到了新策略”，而是确认当前本地标准日线和一分钟 OHLCV/amount 原始通道下，七条看似可继续的路线都属于已冻结旧族的统计量、字段、窗口、路径或模型救援。

## 有限路线

1. 日内收益超额峰度：收益偏度协议与 Campaign110 已明确关闭峰度和尾部集中度替代。
2. 日内 range 轮廓频谱熵：Campaign064 已在值前关闭 range 的 spectral/permutation/transition entropy 选择。
3. 日内 share-volume 轮廓频谱熵：把 Campaign263 的 amount 换成 volume 是结果曝光后的字段替换，且会新增份额单位与复权混杂。
4. amount 方向字典短语数：Campaign120 已在 amount 弱序和事件间隔路线之后明确关闭该替代。
5. 跨日 high-low range 轮廓相似度：Campaign057 与 Campaign100 已保存相同值前拒绝。
6. 零成交或零 range 最长连续段：Campaign105/106 已关闭该二元支持状态的路径和窗口细化。
7. 143 项完整数值库的 pair、调权或模型：属于已曝光终止因子的重新组合，违反无救援边界。

七条路线均为值前科学尝试；选中因子、完整定义追加、数值比较器追加、开发试验和 2024–2025 压力试验全部为 0。完整库保持 162 个定义和 143 个数值比较器，累计历史尝试由 2,597 增至 2,604，累计收益读取开发试验保持 315。

## 研究边界

- 只读取仓库元数据、完整终止历史以及 Parquet footer 的 schema/row-count 元数据。
- 没有读取 Campaign264 候选值、比较值、历史日线价格序列或 forward return。
- 没有请求 provider 或 Web，没有读取凭据值或摘要。
- Candidate49 仍是唯一前瞻候选；信号/执行账本保持 0/0，禁止历史回填和 Candidate50。
- 当前时间早于 2026-08-24 16:30 Asia/Singapore，没有执行 Candidate49 plan/run。
- 没有生成当前评分、选股、仓位、订单或投资建议。

## 下一步

Campaign265 只可从真正新的、被接受的点时信息通道开始，或在值前证明一个机制不属于本轮七条拒绝路线和完整 162 定义库。不能为了保持“迭代数量”而把同一信息换统计量、换字段、换窗口或重做组合。

Candidate49 的未来观察仍是独立确认层；同日工作流仍须在目标交易日 16:30 后先执行只读 `plan`，仅当 `ready=true` 且退出码为 0 才可用完全相同参数执行确认运行。
