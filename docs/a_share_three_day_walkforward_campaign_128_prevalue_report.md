# A 股三日历史滚动研究：Campaign128 值前阶段报告

记录日期：2026-08-14（Asia/Singapore）

Campaign128 已从六项有限目录中唯一冻结 higher 定义 `intraday_transaction_price_dispersion_resolution_2h`，但尚未读取任何历史分钟行或因子值。它在上午、下午各120根连续竞价分钟内，把 `amount/volume` 的对数作为活动分钟成交价、volume作为权重，按左连续累计权重定义Q25/Q75；要求每半日至少60个活动分钟且两个半日IQR均严格为正，最终取 `(morning_iqr-afternoon_iqr)/(morning_iqr+afternoon_iqr)`。越高表示下午成交价分布相对上午收窄。

该机制与收益波动收敛、全天成交价几何/算术共识、全天成交价Bowley偏度和上午/下午金额时钟分布分别做了值前区分。偏度变化、市场残差、活动过滤、金额Gini、季度确认乘积及所有替代分位数、支持、方向、阈值、过滤、模型和组合均已排除。

纯公式通过8个合成测试，覆盖方向、半日交换、成交价单位平移不变性、联合零活动规则、单边零失败、每半日支持、无epsilon的零IQR失败和无效输入；Black与Ruff通过。完整逻辑定义库由150增至151，顺序哈希为 `91c2da3018330edbaddbb60b86370dcd9732425590f0565c70790b990002e0b5`。由于尚无不可变历史快照，数值比较器仍为139，顺序哈希保持 `9c4de054ca1b83eced67eece256b024ebec3a6e1925de17a4cedfa7d36265161`。

当前4次尝试包括2次值前科学尝试，以及程序启动前的shell通配错误和统一报告哈希路径写错这2次基础设施失败；后者未读取目标或改变科学结果，并已追加保留。累计历史尝试1,037，收益读取开发试验保持306。v137只修正追加式会计，不改变公式、151/139冻结库或下一门禁。下一步只允许实现并冻结一个绑定既有不可变分钟源的快照构建器与恢复验证器；在该冻结和单独plan以前不得读取历史分钟分区。之后仍须先过覆盖门，再按冻结顺序比较全部139项，只有通过才能打开唯一2019–2023滚动试验；2024–2025保持关闭。

Candidate49仍是唯一前瞻候选且两本台账均为空。本阶段没有凭据加载、供应商请求、历史回填、第二前瞻候选、当前评分、选股、仓位、订单或投资建议。

## 来源、覆盖和数值去重推进（06:33）

随后冻结的源绑定构建器通过15项合成测试，元数据计划以退出码0和 `ready=true` 通过后，按唯一确认命令原子发布了33,015个分区、7,724,498个基础股票日的不可变快照。7,580,753行满足公式支持，manifest SHA为 `6ddc6358bffbfd6ae412744e96479b7bc6bc179ee0ae20cff9b3cb3e85b86e2f`，数据集摘要为 `4644fdbf8ddbd046def730c9eaf848fdc56874f617ea2aefb50dedcffbc9bb4f`；只读恢复验证逐分区通过。来源只读 `datetime/symbol/provider/volume/amount`，没有provider请求、日线、比较器或收益读取。

结构覆盖门也通过：中位/P05日覆盖为 `99.320113%/98.130841%`，P05有效股票数138，540个不重叠三信号会话cohort，1,632个非恒定横截面会话。随后完整139项有序数值去重全部通过，最大绝对中位日秩相关为 `0.406144`，对应 `intraday_amount_center_of_mass_240m`；不存在失败比较器。

本阶段另追加了三个基础设施失败：恢复验证器未使用导入、覆盖合成夹具年份不足、以及首次唯一性加载未先过滤合法缺失支持。最后一项只重开候选值并在比较器1之前终止；v2仅过滤 `eligible=true` 且有限的候选行，不填充或改变公式，随后完整运行成功。Campaign128当前共11次尝试，累计历史尝试1,044，收益读取开发试验仍为306。下一步只能冻结并激活唯一一个2019–2023三折开发试验；日线、forward return和2024–2025收益仍未打开。

终态全范围回归另保留两项非科学失败：42项通过而6项旧冻结的“输出尚不存在/报告哈希未推进”生命周期断言按预期失败；随后更新当前生命周期测试时，首次补丁上下文因Black换行不匹配而未应用。旧冻结测试没有改写，新终态测试单独绑定当前证据。有效会计为Campaign128 13次尝试、7次基础设施失败，累计历史尝试1,046；科学结论与收益读取开发试验306不变。
