# A 股三日短线 Campaign145：最终覆盖率终止与验证会计

Campaign145 的科学结论不变：唯一冻结的 higher 因子 `intraday_return_amount_cumulative_path_signed_area_238p` 在 33,015 个不可变分钟分区上完成构建和恢复校验，但在点时质量/上市样本的覆盖率门失败。1,331,759 个合格股票日中候选有效 685,849 行；日覆盖中位数为 `52.46% < 95%`，5% 分位为 `20.61% < 90%`，可用股票数 5% 分位为 `49 < 50`。候选虽有 1,631 个非恒定截面、509 个不重叠 cohort 并覆盖七年，仍不足以进入 141 因子唯一性或历史收益阶段。

原快照 manifest 的 `[0,1]` 范围和继承 verifier 常量缺陷通过追加式恢复凭证处理；没有重建、覆盖、填补、裁剪或缩放任何原快照字节。全部分区文件/数据帧哈希、7,724,498 行、3,954,911 个公式有效值和聚合数据集哈希均通过。实际值约在 `1.31e-8` 至 `0.320195`，冻结公式合法域仍为 `[-1,1]`。

最终有效会计为 10 次研究尝试、11 条哈希链记录：1 个完整因子科学尝试、9 个基础设施失败和 1 条零尝试增量的覆盖终止续行；累计历史尝试为 `1250`，收益读取开发试验仍为 `313`。第 9 条基础设施失败来自过宽绑定清单把不含文件指纹的纯失败记录送入绑定验证器，随后 `shasum` 又掩盖了 Python 非零退出；该组合退出码没有被当作通过，验证器也没有修改或削弱。

完整定义/数值比较器保持 `154/141`。新定义只用于未来语义重复控制，数值序列不加入比较器。Candidate49 仍是唯一前瞻候选，两本账本各 0 条且哈希不变；2026-08-14 没有同日重试。统一研究报告保留在第 8 条基础设施失败后的不可变指纹，最终第 9 条会计由本报告、terminal result v3、policy v193、验证记录和活动状态追加承接。

本结果没有读取 Campaign145 的比较因子、日线、forward return 或 2024–2025 压力收益，不产生当前评分、选股、仓位或订单，也不构成投资建议。目标继续 active；Campaign146 可从真正新的有限值前机制离线开始，不得救援 Campaign145。
