# A 股三日短线 Campaign154 行业成员合同前沿值前终止报告

记录时间：2026-08-15 03:02 Asia/Singapore

Campaign154 审查了本地已经存在的 7,803 条 SW2021 一级行业历史成员区间，但没有读取任何成员行或列值。原始采集合同只为 `sw1_three_session_leave_one_out_breadth` 一个假设取得并保存 `in_date/out_date`，明确禁止失败后改做行业价格动量、改变行业层级/窗口/同行阈值或组合；该宽度因子也已在完整历史诊断后终止。

有限目录包含 6 个方向：行业内分钟收益残差方差、行业中性尾盘残差、行业成交额轮廓同步、行业振幅轮廓同步、行业内绝对成交额排名和当前行业成员任期。前五项是在已终止的市场残差、尾盘漂移、市场成交额/振幅同步或 Campaign152 绝对成交额家族中替换分组或重新组合；成员任期虽是不同信息状态，但 `in_date/out_date` 原本只允许用于宽度因子的点时行业分配，不能在看到宽度结果后另立评分合同。

因此 6 项全部在原始来源合同门停止，选中候选为 0。没有创建公式实现、快照、适配器或模型，没有运行来源验收、覆盖率、唯一性、2019–2023 收益试验或 2024–2025 压力阶段。完整定义/数值比较器保持 `157/142`。

Campaign154 追加 6 次科学概念尝试、0 次基础设施失败；累计历史研究尝试为 1,349，累计读取收益的开发试验仍为 314。Candidate49 仍是唯一前瞻候选，两本账均为 0 条；周六没有运行日更、读取凭据或请求 provider，也没有历史回填、当前评分、选股、仓位或订单。

持续目标保持 active。Campaign155 只能从原始合同已允许的新机制开始，或在任何目标源数据被观察前建立独立授权的 source-admission campaign；不得给 SW 历史成员快照事后换合同。

## 回归测试生命周期会计追加

Campaign145–154 的首次跨轮回归为 `71 passed, 1 failed, 1 deselected`。唯一失败来自 Campaign153 测试把可追加统一报告的历史 `sha256_at_publication` 与 Campaign154 追加后的当前字节作相等比较；这是测试生命周期语义错误，不是科学结果或证据绑定变化。非零退出按第 1 次基础设施失败写入追加式台账。修复只让旧测试验证“历史发布哈希保留、报告明确可追加、Campaign153 段落仍恰好出现一次”，随后 Black、Ruff 以及回归 `72 passed, 1 deselected`。Campaign154 最终 7 次尝试，其中 6 次科学概念、1 次基础设施失败；累计历史研究尝试为 `1350`，收益读取开发试验仍为 `314`，`157/142` 库和全部研究边界不变。

最终批量验证命令先成功解析 13 个 JSON，随后因在 zsh 中误用特殊循环变量 `path` 覆盖命令搜索路径，`shasum` 与 `date` 均返回 command not found，组合退出码为 127。该失败作为第 2 次基础设施尝试保留；改用非特殊变量及绝对命令路径后同一解析和哈希步骤退出 0。Campaign154 最终有效尝试为 8 次，累计历史研究尝试 `1351`；科学终态和所有安全边界不变。
