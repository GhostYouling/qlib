# Campaign136 no-return research report

Campaign136 selected exactly one prevalue factor, `daily_realized_price_basis_adjustment_magnitude_1d`, with frozen higher-is-better direction and score `abs(log(factor_t/factor_p))` for the same symbol on adjacent accepted trading sessions. Both rows must use `close_known_raw_pct_chg_chain_v1` from `baostock` and finite positive factors. No sign, lag, window, threshold, tolerance, filter, subset, model or combination was searched.

The immutable 2019–2025 candidate snapshot contains 7,751,950 rows in 5,396 partitions; 7,750,120 rows are eligible, including 299,997 exact-zero pairs. Its dataset SHA-256 is `c84b1f85eec3731736a4da411691c144a70a81e38b5013e5aeea5370f3c417ec`. The first build failed closed on a nullable missing-predecessor identity mask; the versioned recovery maps only that missing identity to false, preserves the original formula, and published no partial output.

The frozen structural gate passed on the quality/current-listing base: median and P05 daily coverage were both 1.0, P05 eligible names was 138, 540 non-overlapping three-signal-session cohorts covered seven observed years, and all 1,632 sessions were cross-sectionally nonconstant.

The ordered uniqueness audit recorded all 140 comparators in the frozen `c71bfe…` order without early stopping. Every comparison passed the strict absolute-median-daily-Spearman `< 0.8` gate. The maximum observed value was 0.03709644736183112 against `intraday_price_update_share_238m`; no comparator was removed or filled.

This is no-return admission, not strategy validation. No historical OHLCV or forward-return field has been read for Campaign136, no 2024–2025 return lockbox has been opened, and no current scoring, selection, sizing, position or order was produced. The only authorized next step is to freeze and single-use activate one inherited three-fold 2019–2023 Top-3 next-open-to-third-close development trial before any return read.

Candidate49 remains the sole prospective ledger. Its signal and execution ledgers remain empty and byte-identical; Campaign136 performed no historical Candidate49 backfill and made no prospective activation.
