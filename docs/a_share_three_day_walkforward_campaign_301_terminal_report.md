# Campaign301 终端报告：Alpha158 与本地日线残余前沿审计

Campaign301 没有强行制造新因子。它在读取任何 Campaign301 候选、比较器、日线价格或 forward return 值之前，对当前仍看似可用的 Alpha158 与本地日线 OHLCV 路线做了 11 项有限审计。结论是：10 项属于已终止家族的换公式、换窗口、换模型或事后组合，1 项 Alpha360 因工作区原子物化空间不足而继续延期；本轮选中候选为 0。

## 审计结果

- K 线实体/影线/收盘位置、趋势/残差、分位/极值、量价相关、涨跌符号矩、成交活跃度、peer/multi-horizon 几何、日线缺口/日内路线均已有明确终止记录。
- Alpha158 完整库上的 LightGBM、固定 MLP、固定随机傅里叶核岭和固定随机超平面分区已在 Campaign286–289 分别终止；线性、稀疏、子集、邻域、核、树重试和超参数救援也已值前关闭。
- 对 172 个旧定义做筛选、配对、调权、阈值、过滤或再拟合，不是新观测状态，而是历史结果曝光后的组合搜索，因此禁止。
- Alpha360 没有被判定无效。当前授权工作区只剩 11,008,628 KiB，无法为五年 360 维稠密快照、原子临时副本和模型输出保留安全余量；不得缩短历史、先看覆盖再选股票，也不得通过外部 `data` symlink 绕开写入边界。

## 终局与会计

Campaign301 记录 11 次值前科学尝试，完整因子尝试、收益读取开发试验和验证折读取均为 0。2024–2025 未打开。完整定义/数值比较器库保持 `172/153`，顺序摘要保持 `ba884534...af0df1` / `41b42563...0b499d`；v431 只发布不追加。

累计历史研究尝试更新为 3,107，累计收益读取开发试验保持 331。Candidate49 仍是唯一前瞻候选，信号/执行账本保持 0/0；2026-08-25 不得同日重试。本轮没有 provider 请求、历史回填、第二前瞻候选、当前评分、选股、仓位或订单。

Campaign301 终止的是“当前可执行的 Alpha158/本地日线残余搜索前沿”，不是整个研究目标。Campaign302 只能从真正新的已接受信息通道，或另行冻结、资源安全且不改变完整语义的机制开始。本报告不构成投资建议。
