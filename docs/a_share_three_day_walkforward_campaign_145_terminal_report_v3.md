# A 股三日短线 Campaign145：最终覆盖率终止、台账与严格验证

Campaign145 冻结的 higher 因子 `intraday_return_amount_cumulative_path_signed_area_238p` 在不可变分钟快照上完成构建、追加式恢复校验和覆盖率审计。33,015 个分区、7,724,498 行、3,954,911 个公式有效值及聚合数据集哈希全部复核通过，原 manifest 和分区没有被重建或改写。

点时质量/上市基数为 1,331,759 个股票日，候选有效 685,849 行。日覆盖率中位数 `52.46% < 95%`、5% 分位 `20.61% < 90%`、可用股票数 5% 分位 `49 < 50`；因此覆盖率门失败。截面变化、cohort 数和观察年份虽通过，也不能越过容量门。141 个比较因子、历史日线、forward return 和 2024–2025 压力收益均未读取。

最终有效会计为 11 次研究尝试和 12 条哈希链记录：1 个完整因子科学尝试、10 个基础设施失败，以及 1 条研究尝试增量为 0 的覆盖终止续行；累计历史尝试 `1251`，收益读取开发试验保持 `313`。最后两条验证失败分别是过宽绑定清单/被后续 `shasum` 掩盖的 Python 非零退出，以及目标验证汇总误用不存在的 `binding_checks` 键。两者均未当作验证通过，也未修改或削弱验证器。

修正后的独立验证以真实 `binding_count` 键完成：30 个 Campaign145/政策/台账 JSON 可解析，12 个含指纹的权威记录共 58 项绑定全部通过。Black、Ruff 和 Campaign145 聚焦测试同时通过（`34 passed`）。完整定义/数值比较器为 `154/141`，新定义仅保留作语义去重；Campaign145 不允许救援或进入收益阶段。

Candidate49 仍是唯一前瞻候选，两本账本各 0 条且哈希不变；2026-08-14 没有同日重试。本结果不产生当前评分、选股、仓位或订单，也不构成投资建议。目标继续 active，后续只能以真正新的 Campaign146 有限机制继续。
