# Campaign168 经营规模、采用与利用率 KPI 来源前沿值前终止（有效会计）

Campaign168 在不读取来源行、文档标题/正文、KPI 值、候选值、比较器、价格或收益的条件下，固定审查 7 条路线。客户/订阅/活跃用户规模、门店/站点/服务资产覆盖、利用率/入住率/客座率/吞吐率，以及实际产销/出货/服务量是 4 个不同的发行人经营状态；但本地没有冻结精确官方来源、首次可用时点、历史修订、身份与期间口径、单位/分母、行业可比性以及 2019–2023 完整覆盖合同，因此仅记为来源准入延期，不宣称无效。

最新快照历史回填、基础 KPI 准入前的增长/季节性/预期差/同业归一化，以及跨四类未准入状态的组合建模共 3 条路线在值前关闭。没有公式、方向、参数、过滤、子集、组合或模型进入完整定义。

初始科学发布记录 9 次尝试（7 科学、2 基础设施）。随后，测试命令假设不存在的 `.venv/bin/python` 而在执行测试前退出；运行时探测又用 `|| true` 掩盖了一个缺失的可选 `uv` 检查。这两项均追加保留，实际 Anaconda Python 3.12.7 与 Black/Ruff/pytest 已用 fail-closed 检查确认。Campaign168 有效会计为 11 次尝试（7 科学、4 基础设施），累计历史尝试 `1474`，收益读取开发试验仍为 `314`；定义/比较器保持 `158/142`。Candidate49 两个空账本未变，2024–2025 未打开，持续目标保持 active。
