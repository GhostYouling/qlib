# Campaign079：信号日换手率（值前机制重叠终止）

Campaign079 在任何日线数据行、候选值、比较值、价格或收益读取前终止。最初选择的 higher 定义 `signal_day_turnover_rate_pct` 是接受供应商的原始信号日换手率百分比；原假设是更广泛的筹码交换与注意力可能延续三日。

后续元数据与历史定义核查发现，权威研究库存已经多次覆盖单字段换手水平、换手变化、`turnover_surge`、`turnover_surge_1`、`turnover_surge_3` 和 `return_turnover_correlation_10`。因此原机制审查的“独立”结论无效；直接测试原始换手水平等同于删除旧窗口/归一化后的历史救援，不是新机制。旧审查保留，纠正记录作为有效结论；禁止在 Campaign079 内改用对数、截尾、滞后、变化、反向、中性化、过滤、模型或组合。

本轮只读取了管线状态、字段契约与 Parquet schema 元数据；没有读取日线数据行，没有构建候选快照，没有运行 109 项数值比较、开发试验或 2024–2025 stress。追加台账记录 1 个完整因子尝试、0 次基础设施失败；累计历史尝试为 484，累计收益读取开发试验保持 280。

v15 将该失败定义追加到完整语义库，因此完整定义为 111 个；由于无候选数值，它不进入数值比较库，比较器仍为 109 个且顺序哈希保持 `0893e0ff54c0094e48e67def84f0038a453fd11230629631e3e54120480a3529`。Candidate49 仍是唯一前瞻候选，信号/执行账本各 0 条；未调用供应商、未历史回填、未生成当前评分、选股、仓位、订单或投资建议。
