# Campaign141 值前零候选终止

本轮事前冻结并完整审查 6 个方向：季度增长相对 ROE 的残差/中性化、预告扭亏与实际盈利确认、机构调研及时性与分析师关注共识、限售解禁压力相对自由流通稀缺性、资产负债韧性与货币资金安全组合，以及关联交易稀疏与标准审计意见确认。

六项均在读取历史值前被拒绝。季度残差是已终止会计状态的算子/模型刷新；预告×实际、调研×分析师、解禁×自由流通、两项资产负债安全和治理×审计，都是已暴露或容量终止分支的事后确认、缩放或组合。它们能由现有字段计算，不等于提供了新的独立信息机制。

因此 Campaign141 没有创建公式实现、特征快照、过滤、子集、组合或模型；没有读取来源行、候选值、141 项比较值、日线、forward return 或 2024–2025 压力收益，也没有发出 provider 请求。6 个值前概念全部写入追加式哈希台账。累计历史尝试为 `1181`，收益读取开发试验保持 `313`，完整定义/数值比较器保持 `153/141`。

Candidate49 仍是唯一前瞻候选，两本账本均为 0 项且未改变。目标继续 active；Campaign142 可在任意本地时间从真正新的有限值前机制继续。历史结果不产生当前评分、选股、仓位或订单，也不构成投资建议。

## 最终会计追加

首次组合验证中，JSON、Ruff 和 5 项聚焦测试通过，但新测试需要 Black 机械格式化。该命令未启用 shell errexit，后续成功命令把组合退出码变成 0；这次被掩盖的格式失败已显式记录，没有被当作通过。应用 Black 后科学结果不变。Campaign141 最终为 7 次尝试，其中 6 个值前概念、1 次基础设施失败、0 个完整因子和 0 次收益读取试验；累计历史尝试 `1182`，收益读取开发试验仍为 `313`。

第二次复验严格启用 errexit，发现 policy v180 的 supersession 副本漏写了两个哈希字符，导致 1 项绑定测试失败；权威输入副本正确。非零退出未绕过，旧政策未改写，改由追加式政策修正。最终 Campaign141 为 8 次尝试，其中 6 个值前概念、2 次基础设施失败；累计历史尝试 `1183`，收益读取开发试验仍为 `313`，科学结果保持值前零候选。
