# A 股三日短线研究交接：Campaign273 v3

Campaign273 已在读值前终止。事前冻结的 7 条路线中，5 条与历史机制重叠；法院执行程序与卫星夜光经营足迹虽经济含义独立，却缺少完整可审计的 2019–2023 点时主体、修订、公开时钟和全市场来源合同。没有生成公式、方向、来源行、因子定义、数值比较器、预测结论或 2024–2025 压力结果，库保持 `162/143`。

当前有效台账为 `research_attempt_ledger_v4.json`：12 次尝试，包括 7 次值前科学路线与 5 次基础设施失败；累计历史研究尝试 2,779，收益读取开发试验 315。聚焦验证为 `7 passed`，Black/Ruff/最终 JSON/diff 全通过，Campaign265–273 与 Candidate49 跨阶段回归为 `240 passed, 5 deselected`。第 5 次失败来自普通 `git add` 被 `data/` 忽略规则拒绝；没有提交发生，恢复时只用 `git add -u` 更新已跟踪报告，并精确强制加入本轮台账。v1/v2 交接与早期状态不改写，本文件是当前交接口径。

Candidate49 仍是唯一前瞻候选。2026-08-24 的 `stock_basic_invalid_ts_code` 已失败关闭，不得同日重试或重请求；信号/执行账本保持 0/0，禁止历史回填和 Candidate50。`.env` 只验证为普通 0600、Git 忽略、恰有一个非空 Token 赋值，秘密未打印、散列、记录或暂存。

持续目标保持 `active`。Campaign274 只能先冻结新的、有限且真正独立的概念目录，再做针对性历史前沿审查；不得以来源、司法辖区、主体子集、指标、窗口、阈值或方向变体救援 Campaign273，也不得把历史结果用于当前评分、选股、仓位、订单或投资建议。
