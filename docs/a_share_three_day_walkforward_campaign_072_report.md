# A 股三日历史 Walk-Forward：Campaign072

Campaign072 已在 no-return 唯一性门禁终止。候选为 higher `quarterly_joint_profit_revenue_growth_floor_rank`：季报只在公告日后的首个接受交易日生效，`profit_yoy` 与 `revenue_yoy` 独立前向传播；在两字段共同有限的股票日截面上先取原始较小值，再计算 average-tie 百分位排名。该公式衡量利润与收入共同增长的保守下限，没有搜索方向、权重、阈值、过滤、窗口、子集、模型或组合。

快照共 33,015 个分区、7,724,498 行，其中 7,231,483 行有有限因子值，并已逐分区完成字节哈希、帧哈希与聚合 dataset 哈希复验。质量与上市过滤后，候选覆盖 1,330,171 / 1,331,759 行；覆盖率中位数 99.9452%，P05 99.5689%，P05 可用股票数 138，可形成 540 个不重叠三日 cohort，覆盖门禁通过。

随后按冻结顺序完成全部 102 个数值比较，99 项通过、3 项失败：与 `quality_growth` 的中位日秩相关为 +0.913246，与 `quality_revenue` 为 +0.898448，与 `quality_score` 为 +0.827920；三者各有 1,623 个共同会话，均超过严格的绝对 0.8 上限。原始值先取最小再排名，仍未在数值上摆脱既有质量增长轴，因此语义差别不足以进入收益研究。

审计实现共经历 4 条失败运行命令：前两条在覆盖前分别暴露双层运行时覆盖未同步和缺少继承兼容常量；后两条在覆盖通过、比较值已读取后分别暴露中间层 101/最终层 102 计数边界和内层追加函数寻址错误。所有退出码与“是否已读比较值”边界均以版本化记录保留；v4/v5 没有把已经发生的比较读取错误写回 `false`。另有 5 条事前只读侦察命令和 1 条失败后目录诊断命令失败。合计 10 次基础设施失败和 1 次完整因子尝试，使累计历史研究尝试从 426 推进到 437；累计读取开发收益的试验仍为 275。

因此未创建开发预注册，未读取任何日线价格或前瞻收益，未运行 2019--2023 开发折，也未打开 2024--2025 准样本外区间。Campaign072 不得通过反转、缩放、残差化、过滤、删除比较器或同轮组合来补救。Candidate49 仍是唯一前瞻候选，信号与执行账本均为 0 条，本轮没有 provider 请求。

限制：历史股票池来自当前上市快照，存在存续偏差；季报数据可能含后续修订。本结果只是冗余性拒绝，不是收益证据或投资建议。
