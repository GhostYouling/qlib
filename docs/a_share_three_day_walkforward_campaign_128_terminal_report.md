# Campaign128 历史滚动终局

`intraday_transaction_price_dispersion_resolution_2h` 比较上午和下午成交价分布的成交量加权 IQR：每半日按 `log(amount/volume)` 的左连续加权 Q25/Q75 求离散度，并以 `(morning_iqr-afternoon_iqr)/(morning_iqr+afternoon_iqr)` 衡量午后收敛，方向 higher。不可变快照含 33,015 个分区、7,724,498 行，其中 7,580,753 行有效；覆盖中位/P05 为 `99.320113%/98.130841%`。139 项冻结数值比较全部通过，最大绝对中位日秩相关为 `0.406144`。

唯一 2019–2023 开发试验的正 mean Rank-IC / normalized-return / 10bp pilot-return 折数为 `0/1/1`。验证中位 mean Rank-IC 为 `-0.824094%`，normalized return 为 `-25.227148%`，10bp pilot return 为 `-3.657304%`，最差 normalized drawdown 为 `-34.339018%`。全开发期整手 pilot 在 0/5/10/20bp 分别为 `+1.423566%/-1.856460%/-5.652441%/-12.789372%`。

因子同时未通过关联/收益折一致性、验证中位关联/成本、回撤和 20bp 聚合成本门；第 1、2 折整手可负担率还低于冻结下限，因此操作门也未通过，survivor 为 0。2024–2025 压力区保持关闭且未读取。终局组合验证中 6 条已冻结的构建前生命周期断言在合法存在的快照/覆盖/唯一性产物上失败，阶段适配验证随后 `12 passed, 1 skipped`；旧测试未改写，科学结论未变。最终状态生成的三项只读诊断错误也逐项恢复并追加。Campaign128 最终记录 18 次尝试、11 次基础设施失败、1 个完整因子和 1 次收益读取开发试验；累计历史尝试为 1,051，累计收益读取开发试验为 307。

v144 保留 v142 的 151 个完整定义和 140 个未来数值比较器，仅用于重复控制。本因子自身永久终止，不得反向、改分位数/半日支持/零量额语义、过滤、挑年份、拟合、重跑、组合或救援。Candidate49 仍是唯一前瞻候选且两本账均为 0 条；没有历史回填、第二前瞻候选、当前评分、选股、仓位、订单或投资建议。
