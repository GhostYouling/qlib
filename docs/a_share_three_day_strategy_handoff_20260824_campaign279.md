# A 股三日短线研究交接：Campaign279

Campaign279 已完成并在公式、方向、来源行和数值之前终止。事前冻结的 7 条公共外部状态路线中，文化遗产许可、医疗准入、水权、动物防疫、出口管制和生态标签 6 条与既有不可变终止机制重叠；集体协商/工会协议/劳资争议虽经济独立，但缺少完整、合法、可审计的 2019–2023 点时来源合同。没有形成新因子定义、数值比较器或预测结论，2024–2025 未打开，定义/比较库保持 `162/143`。

本轮权威结果是 `docs/a_share_three_day_walkforward_campaign_279_prevalue_mechanism_frontier_result_20260824.json`，SHA-256 为 `3cce302ad78615cfc69d3efed199110921cd2a4ce8519f5d4a8dab2d9a0cc1db`。当前追加审计账本是 `data/experiments/short_horizon/historical_walkforward/campaign_279/research_attempt_ledger_v1.json`，SHA-256 为 `5e98cd6fda79397eefae01ace4dd5b039bf3b540806e6c1087190648b9e302db`；有效会计为 8 次尝试：7 次值前科学尝试、1 次纯基础设施失败，累计历史尝试 2,851，收益读取开发试验保持 315，链尖为 `3c86ed71b15f79800a78201d3daf74d8c1dd60d3d7aff7af5eea94da0ffee6e0`。

唯一基础设施失败发生在研究值读取之前：初始权威状态哈希命令使用了两个不存在的 Candidate49 旧式嵌套路径并非零退出。修正只使用跟踪文件名找到 `candidate49_future_signal_ledger.json` 和 `candidate49_future_execution_ledger.json`，确认条目仍为 0/0、哈希仍为 `5193f00d…` / `d57a3e61…`；失败输出未作为通过或科学证据。

Candidate49 仍是唯一前瞻候选。2026-08-24 的 `stock_basic_invalid_ts_code` 保持失败关闭，不得同日重试或补请求；禁止历史收益/信号/执行回填和 Candidate50。活动数据根仍是 `/Volumes/DIsk/Disk-Coding/qlib/data`；`.env` 只验证普通文件、0600、Git 忽略和一个非空 Token 赋值，秘密值或摘要未读取、打印、记录或暂存。

持续目标保持 `active`。下一轮 Campaign280 只能先冻结新的有限概念目录，再做零网络的针对性历史前沿检查；不得用当前快照、不完整覆盖、兄弟提供方、主体角色、辖区、字段、指标、窗口、阈值或方向救援 Campaign279。历史结果不得生成当前评分、选股、仓位、订单，也不构成投资建议。
