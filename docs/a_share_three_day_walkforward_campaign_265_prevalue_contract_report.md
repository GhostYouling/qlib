# Campaign265 convertible-premium prevalue contract report

Campaign265 advanced one previously deferred cross-asset mechanism without opening a provider row or research value. The complete 162-definition and 143-comparator history was reviewed first. Of six finite routes, one exact mechanism was selected and five level/direction, bond-price, liquidity-interaction, subtype-expansion and model-rescue alternatives were rejected before values.

The selected higher factor is `convertible_bond_equity_parity_premium_compression_3s`. For accepted A-share signal session `t`, each exact `CB` instrument must have a finite Tushare `cb_over_rate` and strictly positive `amount` at `t` and the accepted A-share session three entries earlier. The per-bond value is `cb_over_rate(t-3) - cb_over_rate(t)` in percentage points; multiple bonds mapped to one equity use the deterministic arithmetic median. No active or eligible bond is missing, never zero or forward-filled.

This is economically different from Campaign127's one-time primary issuance subscription demand. It observes the secondary-market wedge between a listed convertible and equity-conversion parity. It does not use bond OHLC/returns, equity prices, liquidity magnitudes, ratings, redemptions, issuance fields, accounting values, historical comparators, models or terminal factor combinations.

Current official Tushare documentation identifies `cb_basic` for the bond-to-equity mapping and listing interval, and `cb_daily` for daily `cb_over_rate` and `amount`. Both have a 2,000-point minimum; 5,000 points only provide relatively higher request frequency and are not required for this contract. No Token was loaded and no API response was requested. The exact URLs, fields, 2019–2025 scope, 17:00 information clock, three-session lag, missing/stale semantics, active-CB denominator, 95%/90% coverage, 50-name support, 200-cohort/five-year capacity and all 143 ordered uniqueness gates are frozen in the source contract.

This stage does not establish predictive value, append a complete factor definition, add a numeric comparator, or authorize a source call. The next allowed step remains inside Campaign265: implement a deterministic zero-network adapter and synthetic tests, then freeze a separately confirmed one-shot acceptance workflow. Only a successful immutable source acceptance and all no-return gates may later open 2019–2023 development. 2024–2025 stays closed.

Six scientific attempts and one pre-write publication-patch failure were appended to the local hash-chain ledger, bringing cumulative historical attempts to 2,611 while return-reading development trials remain 315. The failed patch changed no target file and supplied no scientific evidence. Candidate49 remains the sole prospective candidate with empty signal and execution ledgers; no current score, selection, position, order or investment recommendation was produced.
