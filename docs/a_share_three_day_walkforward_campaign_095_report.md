# Campaign095：自身 K 线收盘位置熵（值前语义终止）

Campaign095 在任何分钟源行、候选值、比较值、日线价格或收益读取前终止。冻结的 higher 定义 `intraday_own_bar_close_location_entropy_10b_240m` 原计划把正振幅分钟的自身 K 线收盘位置分入十个等宽桶，并计算归一化 Shannon 熵，以衡量局部成交接受位置的多样性。

合成测试发现，冻结的 `floor(10*x)` 半开桶规则在 `0.1、0.2…` 等精确内部边界上不满足机制审计声称的无条件反射不变性：原序列熵为 `0.99608762`，反射序列为 `0.99462907`。测试结果为 5 通过、1 失败。该冲突属于公式与语义声明不一致，不能通过改边界、删测试或弱化声明在同一 campaign 内修复。

因此没有创建特征快照，没有加载 124 个比较器，没有运行 2019–2023 开发试验，也没有打开 2024–2025。追加台账记录 1 个完整因子尝试、0 次基础设施失败；累计历史尝试为 659，累计收益读取开发试验保持 291。

v39 将该冻结失败定义追加到完整语义库：完整定义 127 个，顺序哈希 `17ad9beb53d236171155211ea4cc4b9ec05b8edea7ebc51ca7705f84adc9e030`。因为没有数值快照，它不进入数值比较库；比较器仍为 124 个，顺序哈希保持 `d05f1cb7b68e950d508f2b0d13b7ef1847904edc6eb10453405917e04ed0e698`。

Candidate49 仍是唯一前瞻候选，信号/执行账本各 0 条；本轮未调用供应商、未做历史回填、未生成当前评分、选股、仓位、订单或投资建议。历史结果仍受当前上市快照的幸存者偏差限制。
