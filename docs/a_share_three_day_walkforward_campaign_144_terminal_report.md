# A 股三日短线 Campaign144：组合前沿值前终止

Campaign144 继续围绕用户要求的“因子排列组合”做有限值前审查，但没有把已失败的完整库模型换一个阈值再跑。本轮六条路线包括：141 因子相关组件均衡、语义分组均衡、共识减分歧惩罚、隔日分钟收盘位置轮廓相似度、申万一级行业内分钟收盘位置同步度，以及成交价量权中心相对时权中心的位移。

三条完整库路线都未获得新试验资格。相关组件均衡就是 Campaign134 在 140 因子上已运行的同一模型家族；该试验在读取第 1 折验证收益前就因与 66 因子算术共识的绝对中位日秩相关达到 `0.818031` 而失败。新增第 141 个比较器不允许刷新组件数、阈值或图。语义分组和共识减分歧则分别属于 Campaign134/135 已关闭的重分组、重加权与稳健聚合算子刷新。

三条来源机制也未通过。隔日收盘位置轮廓只是把 Campaign056/057 的精确前一日轮廓算子与 Campaign086 的收盘位置状态事后重组；行业同步度曾被 Campaign099 作为基准搜索分支事前排除，且 SW 行业来源合同把独立因子数冻结为 1，并在行业广度终止后禁止另做行业价格因子；成交价量权/时权中心位移则改变了 Campaign007 与 Campaign043 已暴露的成交价层级、权重与中心选择。

因此本轮选中 0 个候选，没有创建公式、模型、快照或适配器，没有读取来源列值、候选/141 项比较值、日线、forward return 或 2024–2025 压力收益，也没有发出 provider 请求。定义/比较器保持 `153/141`，Candidate49 仍是唯一前瞻候选，两本账本各 0 条且哈希不变；2026-08-14 没有同日重试。

追加式台账最终记录 28 次尝试：6 个值前科学概念和 22 个基础设施失败。原 12 个失败包括 zsh 无匹配通配符、过宽外部 manifest 扫描、未来时间标签、错误路径、缺失仓库 `.venv`、Black 首检和补丁上下文；发布复核又如实追加 10 个运行时/时间戳/格式检查失败，包括两个 Python 运行时均缺少 Black、Ruff、Pytest，Anaconda Pytest 首次缺少仓库 `PYTHONPATH`，policy v189 与首个活动状态的未来时间标签，以及终态测试切换到新账本后的 Black 首检。所有非零退出均未绕过，也没有用后续成功掩盖；改用仓库已有 Anaconda 工具、设置 `PYTHONPATH=.` 并格式化未冻结测试后，Black、Ruff 和聚焦测试通过（`6 passed`）。累计历史尝试更新为 `1240`，累计收益读取开发试验保持 `313`。

Campaign144 在值前终止不代表历史样本不能继续研究，而是当前六个提案没有新增信息或授权边界。目标继续保持 active；Campaign145 仍可在任意本地时间离线推进，但必须从一个原始采集合同允许、已有历史版本化点时来源支持的真正新机制开始。若要打开独立的新来源验收，需要用户明确授权。
