# Campaign115：验收 runner 运行时自绑定

一次性 `limit_list_d` runner 现在不仅由文档记录 SHA‑256，还会在 `plan` 和 `run` 中、读取凭据之前，重新计算并核对 runner 本体、测试、协议和纯适配器的指纹。冻结文件缺失、格式错误，或任一代码/测试被改动，都会令 `ready=false`。新增的篡改冻结文件测试已通过。

当前适配器与 runner 定向测试为 20 项通过。真实零网络 plan 显示运行时绑定、日历、4592 只点时可买股票、`.env` 安全属性及 Token 存在性全部通过；唯一 blocker 仍是用户尚未明确确认当前账户已有 5000 积分，因此退出码为 2，供应商请求、成功清单和失败记录均为 0。

本次只是值前运行时完整性加固，没有改变公式、方向、来源、日期、门槛或台账计数；完整定义仍为 141，数值比较器仍为 134，历史收益读取试验仍为 302。Candidate49 两本前瞻账本不变。
