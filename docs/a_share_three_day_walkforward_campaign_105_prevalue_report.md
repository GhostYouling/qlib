# A 股三日短线 Campaign105：成交活跃分钟占比值前预注册

Campaign105 从 5 个有限构想中选出唯一 higher 候选 `intraday_active_trading_bar_share_240m`。候选只读取 `datetime/symbol/provider/volume/amount`：在严格保留 09:30、但公式只选取 09:31–11:30 与 13:01–15:00 共 240 根分钟栏的前提下，要求每根 `volume` 与 `amount` 有限且非负、并满足严格零值等价。两者同时为零是有效的不活跃分钟，两者同时为正是活跃分钟，单边为零则整只股票当日缺失；分数为活跃分钟数除以 240，方向 higher。公式不使用价格、成交规模、网格校验后的分钟顺序、旧因子值、日线价格或未来收益，也不设置最少活跃分钟数。

值前机制审计按 v62 的冻结顺序复核了全部 134 个历史逻辑定义，并逐项挑战了已知失败支持谓词。候选与价格更新、成交量/金额轮廓、成交均价、换手率和 Candidate49 的机制语义不同；Campaign008 的失败支持也未继承，因为任意数量的量额共同零分钟都保持有效，只有源语义不一致的单边零失败。上述结论只允许候选进入实现与无收益审计，不构成数值独立性或收益有效性的证明。

覆盖门和全部 131 个数值比较器已经按顺序冻结：先要求日覆盖中位数至少 95%、P05 至少 90%、P05 合格股票至少 50、三信号会话 cohort 至少 200 且至少覆盖 5 年；只有覆盖通过后，才允许按完整顺序读取比较值，并要求每项至少 50 个同日配对股票和 100 个合格会话、绝对中位日 Spearman 严格小于 0.8。任何失败都会永久终止本候选，不允许反向、改窗、改阈值、删比较器、过滤、残差化、重拟合、救援或组合。

若无收益门全部通过，开发目录也只有一次预注册的 2019–2023 单因子 higher 试验：三个扩展训练/验证折、边界清除三个信号会话，且 t+1 开盘与 t+3 收盘必须留在同一分区。只有这唯一试验通过所有冻结开发门，2024–2025 才可对整个 campaign 一次性打开，并标为历史已暴露的准样本外而非纯净留出。

截至本记录，Campaign105 没有读取分钟源行、候选值、比较值、日线价格或未来收益，没有创建特征快照，也没有运行开发或压力试验。预注册初始节点的累计历史尝试为 778；追加式当前台账含 4 次尝试，其中 3 次为值前基础设施失败、1 次为完整预注册尝试，累计历史尝试更新为 779，累计读取开发收益试验保持 299。新增测试首次 Black 检查失败后仅机械格式化，冻结白名单复跑为 `25 passed`。Candidate49 两本账仍各 0 条，2026-08-08 为周六，没有供应商请求、历史回填、Candidate50、当前评分、选股、仓位、订单或投资建议。当前上市快照仍有存活偏差限制，后续历史结果必须继续显式标注。
