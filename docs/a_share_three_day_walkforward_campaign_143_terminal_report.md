# A 股三日短线 Campaign143：合同范围与语义前沿值前终止

Campaign143 完成了新的有限值前目录审查，目标仍是三日持有期的历史滚动研究，但本轮没有为了增加试验数而打开任何历史值。六个构想为：Tushare 四档订单金额参与熵、季度利润/收入增速分歧绝对值、分钟成交价相对区间中点接受占比、收益方向×金额状态互信息、资产负债去杠杆变化，以及申万一级行业内季度利润增速离散度。

最接近“新信息”的四档订单参与熵在代数上确实不同于旧的大单净流入方向，但这份 2019–2025 历史是在“独立因子数恰为 1”的原始冻结合同下采集，终止记录也关闭了分档类别占比支路。因此不能事后用同一批合同受限字段打开第二个因子。其余五项分别是已终止季度增长关系的绝对变换、成交价接受参考点替换、Campaign116 金额条件方向状态的互信息换算、资产负债韧性的单期差分，以及终止行业 peer 与季度增长的跨源重组。

六项均未同时通过原始合同允许、完整历史已接受、公开可用时钟明确、与完整 `153` 定义语义独立四项门禁。因此 Campaign143 在来源行、候选值、`141` 项比较值、日线、forward return 和 2024–2025 压力收益之前终止；没有创建公式、快照、适配器、过滤、子集、组合或模型，也没有发出 provider 请求。定义/比较器保持 `153/141`。

本轮追加式台账共 14 条：6 个值前科学概念和 8 个基础设施失败。失败包括 zsh 无匹配通配符、错误假定存在前序 finalizer、未保留 yielded 会话、一个预发布合同指纹抄写错误、绑定验证范围错误、错误报告形状假设、过窄的失败记录断言，以及 zsh 只读变量名冲突。每项都发生在研究值之前，均未绕过或用后续成功掩盖；有效 v2 机制审计通过 11/11 绑定，14 条台账链通过 15/15 绑定。累计历史尝试更新为 `1207`，累计收益读取开发试验仍为 `313`。

Candidate49 仍是唯一前瞻候选，两本台账均为 0 条且哈希不变；本轮没有历史回填，也没有第二个前瞻候选、当前评分、选股、仓位、订单或投资建议。当前上市快照的幸存者偏差限制继续保留。目标保持 active；严格发布验证通过后，Campaign144 可以在任意本地时间继续，但仍只能使用原合同允许且已接受历史点时来源支持的真正新机制；另行来源验收需要用户明确授权。

## 终态验证会计追加

严格发布阶段又显式记录 5 个非科学失败：误用不存在的绑定校验 CLI、新测试首次 Black 检查失败、一次追加 ledger 前驱哈希抄写错误、内联 Python 汇总命令的引号语法错误，以及 Campaign142 旧状态对两个可追加统一报告的生命周期兼容性断言失败。所有失败均发生在读取研究值之前，非零退出没有绕过；错误 ledger v3 保留为无效记录，v4 从正确前驱重建，随后继续追加。

当前有效 Campaign143 链为 19 条尝试，其中 6 个值前机制审查、13 个基础设施失败、0 个完整因子、0 次收益读取开发试验；累计历史尝试 `1212`，累计收益读取开发试验保持 `313`。修复后 Black、Ruff 以及 Campaign142/143 聚焦测试均通过，pytest 为 `12 passed`。定义/比较器仍为 `153/141`，Candidate49 两本空账本仍未变化。
