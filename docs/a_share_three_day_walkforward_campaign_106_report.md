# Campaign106 历史滚动研究报告

Campaign106 唯一冻结的 higher 因子是 `intraday_nonzero_range_bar_share_240m`。对 09:31–11:30、13:01–15:00 的精确 240 根分钟 bar，要求 high/low 有限、严格为正且 `low<=high`；`high>low` 记 1，`high==low` 记 0，最终取 240 根的均值。09:30 只参与 241 行源网格身份校验，不进入公式。因子不使用振幅大小、open、close、volume、amount、旧因子、日线价格或未来收益，也没有阈值、过滤、替代方向、窗口、模型或组合搜索。

## 冻结与快照

候选来自 5 项有限事前机制目录，并在读取 Campaign106 分钟源行、候选值和比较器值前通过 135 个完整定义及已知失败支持的机制审计。`.env` 只做结构检查：普通非符号链接文件、权限 `0600`、Git 忽略、恰有一个非空 `TUSHARE_TOKEN`；没有打印、哈希或记录 token。2026-08-08 是周六，本轮没有任何 provider 请求或 Candidate49 workflow。

不可变快照包含 33,015 个分区、7,724,498 行且全部值有效；manifest SHA-256 为 `0fa2bb014e0c51e7ad2254c479f21c2d5a1d32bec57be956a111f2ca5da3c572`，dataset SHA-256 为 `f52544dbce026cf9fca9489c85d80bb6fb69e5e880f5b7bf14fb82f09ae8de0c`。全分区字节哈希、数据帧哈希、离散 `1/240` 倍数语义和聚合摘要均通过复核。底层读取字段严格为 `datetime,symbol,provider,high,low`；没有比较器、日线或收益读取发生在快照阶段。

## 覆盖门与基础设施终局

质量/当前上市口径有 1,331,759 行，候选有效 1,330,171 行；1,632 个交易会话的覆盖中位/P05 为 `99.945175%/99.568865%`，P05 有效名称 138，可形成 540 个不重叠三会话 cohort，覆盖 2019–2025 七个年份。因此 coverage 门明确通过。

首次无收益审计在 coverage 通过后完成前 131 项冻结比较，但第 132 个 `intraday_active_trading_bar_share_240m` 进入通用加载器时没有注册其既有 `[0,1]` 值域，触发 `KeyError`。原 runner 和冻结文件未改写；该失败单独记录。随后只允许一次的 additive recovery 在值前合成测试通过后补充该范围注册，但又发现第 132 比较器有 47 个合法缺失值，而被调用的 `_sorted_candidate_arrays` 是候选专用有限值排序器，会拒绝 NaN。第二次进程同样 fail-closed，且没有发布完整或部分审计结果。

由于恢复激活明确限制为单次，本轮不再临场添加第三个补丁。前 131 项在失败进程中算出的相关值没有被发布或用于门禁结论，第 132 项相关没有计算，因此不存在 132/132 唯一性通过或失败结论。Campaign106 的科学状态是 `inconclusive_due_to_infrastructure`，不是 admissible，也不是 uniqueness rejection；2019–2023 开发试验未开始，2024–2025 压力区间未打开，日线和 forward return 均未读取。

## 会计与后续规则

追加式台账有 3 条：1 个完整事前因子尝试和 2 次基础设施失败；科学决定数为 0，收益读取开发试验为 0。累计历史研究尝试为 787，累计收益读取开发试验保持 300。

v64 将该公式追加到完整语义定义库用于以后防重复，库由 135 增至 136；因为全 132 唯一性结论不存在，数值比较器仍保持 v63 的 132 个及原顺序。Campaign107 及以后新增硬门槛：每个 raw comparator 的值域必须值前注册；合成 fixture 必须同时覆盖有限值和合法缺失；比较器对齐必须保留 NaN 给成对重叠门处理；禁止把 candidate-only finite sorter 用在 raw comparator。

Candidate49 仍是唯一前瞻候选，两本账各 0 条；本轮没有历史回填、第二前瞻候选、当前评分、选股、仓位、订单或投资建议。

### 终态会计追加纠正

终态发布后的只读核对发现：Campaign105 的累计尝试为 786，而 Campaign106 v3 台账已有 3 次尝试，旧报告却只推进到 787，漏计了台账中已经保留的 2 次基础设施失败。本次发现本身按既定追加式口径再记 1 次基础设施/记录失败；因此 Campaign106 当前有效口径为 4 次尝试（1 次事前科学尝试、3 次基础设施失败），累计历史研究尝试纠正为 790，累计收益读取开发试验仍为 300。原终态、台账和 v64 均不改写；v4 台账、终态 v2 与 v65 仅纠正会计，科学未决结论、136 个完整定义、132 个数值比较器及全部研究边界不变。

首版纠正测试随后错误假设终态、政策与状态使用同一个开发试验字段名，组合结果为 `8 passed, 1 failed`；三处实际值均为 0，没有研究值冲突。该测试与 790 中间节点全部保留，并追加为第 4 次基础设施失败。当前最终有效口径是 Campaign106 共 5 次尝试（1 次科学、4 次基础设施），累计历史尝试 791、累计收益读取开发试验 300；v66 继续原样保持 136/132 顺序和基础设施未决科学结论。
