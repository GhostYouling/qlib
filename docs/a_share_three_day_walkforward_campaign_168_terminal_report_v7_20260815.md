# Campaign168 经营规模、采用与利用率 KPI 来源前沿值前终止（最终回归恢复会计）

Campaign168 的 7 条科学路线保持 4 条来源延期、3 条值前拒绝、0 入选。没有读取 KPI、候选、比较器、价格、收益或 2024–2025。

全部 9 次基础设施失败均追加保留；新增失败是累计回归给 3 个冻结 deselect node id 错加 `tests/` 前缀，导致预期排除节点实际运行。恢复将使用 pytest 收集到的精确无前缀 node id。

Campaign168 最终有效会计为 16 次尝试（7 科学、9 基础设施），累计历史尝试 `1479`，收益读取开发试验仍为 `314`；定义/比较器保持 `158/142`。Candidate49 空账本未变，目标保持 active。
