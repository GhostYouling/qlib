# Campaign265 来源验收执行工作流计划报告

记录时间：2026-08-24 13:28:18（Asia/Singapore）。本报告完成当前 Campaign265 的凭据安全执行工作流实现与零网络计划阶段，不代表因子已通过历史收益检验，也不授权真实来源请求。

## 当前策略

唯一冻结定义仍为 `convertible_bond_equity_parity_premium_compression_3s`，方向 higher。对信号会话 `t` 和前三个接受的 A 股会话 `t-3`，同一只 exact `CB` 必须在两端都有有限 `cb_over_rate` 与严格正 `amount`；单债分数为 `cb_over_rate(t-3)-cb_over_rate(t)`，同一正股的多只合格转债取确定性算术中位数。无活动或合格转债为缺失，不置零、不前向填充、不换窗口。

这一定义、字段、方向、三会话滞后、17:00 点时规则、映射、缺失、活动区间、覆盖门和 143 项唯一性门没有因执行工作流实现而改变。完整定义/数值比较库保持 `162/143`。

## 本阶段完成内容

执行协议事前冻结后，实现了 `scripts/a_share_three_day_walkforward_campaign265_source_acceptance_workflow.py`。当前公开 CLI 只有 `plan`；只有未来有效 v420 精确绑定工作流、测试、协议、实现冻结和本次 plan-result 字节时，解析器才可能暴露 `run --confirm-run`。

未来执行状态机固定为：所有既有目标父级必须是仓库内真实目录，父级符号链接会在任何写入或凭据访问前失败；独占创建并 fsync 0600 intent 与 journal 后才允许加载 `.env`；每个请求先 fsync `request_authorized`，再进入 provider；响应事件不保存行数或值；规范化 checkpoint 与 sidecar 经过 fsync、原子替换和哈希验证后才提交；仅允许从精确、完整、可验证的已提交前缀恢复。任何已授权但未提交请求、凭据、传输、schema、空响应、截断、身份、日期映射或持久化失败均终止且禁止重请求。失败记录不保存秘密、摘要、原始值、行数或 provider 明文异常。

v419 授权后，唯一一次零网络 workflow `plan` 实际退出码为 0：

- `ready=true`，34/34 检查通过，无 blocker，目标目录树安全检查通过；
- 2019-01-02 至 2025-12-31 共 1,699 个接受日；
- 精确请求序列为 1 次 `cb_basic` 与 1,699 次 `cb_daily`，共 1,700 次；
- 请求序列 SHA-256 为 `3f9cccb3134b7e9e22f5d34fd8bc07f89e1d8047dd775d07c3257443a97b4c58`；
- intent 未构造，`.env`/环境/token 未由 plan 检查，provider 未导入/创建/请求，目标文件未写入；
- `future_run_authorized=false`，`run_interface_exposed=false`。

## 验证与会计

Black、Ruff 均通过。执行工作流 13 项测试全部通过；Campaign265 合同、adapter、planner、当前状态与 workflow 跨阶段套件为 `60 passed, 1 deselected`，排除项仅是旧 adapter 阶段绑定统一报告发布时字节的历史生命周期断言。v7–v12 追加链通过，最终链尖为 `ac77d8fa218e43aac718c72195b88e8252aa55813a2c651988241594e3ecc900`。

本阶段把全部失败保留为证据：最终 Campaign265 为 31 次尝试，其中 14 次值前科学尝试、17 次基础设施/验证失败、完整因子尝试 0、收益读取开发试验 0；累计历史尝试 2,635，累计收益读取开发试验仍为 315。没有用失败命令作为通过证据。v1 工作流及计划作为前驱证据保留，v2 在任何 run 权限或来源写入前完成父级符号链接加固并重新计划。

`.env` 仍为 Git 忽略的 0600 普通非符号链接文件，只有一个非空 `TUSHARE_TOKEN` 赋值；验证没有打印或哈希其值。Candidate49 仍是唯一前瞻候选，信号/执行账本均为 0 条且哈希不变。

## 结论与下一门

当前策略的“来源合同 + 纯 adapter + 精确一次性执行状态机 + 零网络 plan”已经完成并可复现。尚未完成的是来源取数、覆盖/唯一性审计及历史收益验证，因此不能宣称因子有效。

下一步仍属于 Campaign265，但必须单独发布 v420，精确绑定本次最终字节并取得明确 `--confirm-run` 后，才允许一次 1,700 调用的来源验收。来源成功后也必须先过冻结覆盖门和 143 项有序唯一性门；全部通过前禁止读取价格或 forward return。2019–2023 只有一次冻结开发机会，且 survivor 非零前不得打开 2024–2025。任何历史结果都不得直接生成当前评分、选股、仓位、订单或投资建议。
