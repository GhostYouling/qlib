# A 股三日短线 Campaign142：值前来源与公开时钟门禁终止

Campaign142 已完成一轮新的有限值前审查，但没有为了“继续迭代”而强行读取历史值。六个构想分别是：实际净利润符号转移、利润表/现金流量表披露间隔、申万行业进出最近性、内部人交易实际披露延迟、稀疏公司事件同日拥挤稀疏度，以及分钟振幅序列持续性。

这六项没有任何一项同时满足三个必要条件：机制独立、2019–2023 完整点时来源已经接受、历史公开时钟可识别。净利润符号转移和振幅持续性是终止字段/机制的阈值或统计量刷新；现金转换只有验收样本而完整历史来源失败；行业进出只有生效日期而没有历史公告公开时钟；内部人来源只有保守合成可用日而非真实披露时间；事件拥挤需要在已暴露的稀疏事件家族之间事后选源，不能补出缺失 cohort。

因此本轮在任何来源行、候选值、141 项比较值、日线、forward return 或 2024–2025 压力收益之前终止。没有创建公式、快照、适配器或模型，也没有供应商请求。完整定义/数值比较器保持 `153/141`；Campaign142 记录 6 个值前科学尝试和 1 个基础设施失败，累计历史尝试为 `1190`，累计收益读取开发试验仍为 `313`。

基础设施失败是安全凭据存在性检查命令的 shell 引号解析错误；命令正文没有执行，失败退出码未被绕过。随后只读重试确认权威工作树、私有且 Git 忽略的 `.env` 和恰好一个非空 Token 赋值，但没有打印、散列、记录或持久化秘密。

Candidate49 仍是唯一前瞻候选，两本台账均为 0 条且哈希不变。当前时间早于 16:30，因此没有运行同日 Candidate49 plan/run。目标继续保持 active；下一轮只能从真正新的、已有历史版本化点时来源支持的有限机制继续，另行来源验收则需要明确授权。这里没有当前评分、选股、仓位、订单或投资建议，当前上市股票快照的幸存者偏差限制继续保留。

## 终态验证会计追加

首次严格验证的 JSON、Black 与 Ruff 检查通过，但请求了不存在的 Campaign141 `_v3` 测试文件，pytest 在收集阶段以退出码 4 停止且运行 0 项测试。其后一次三报告补丁因统一报告上下文不匹配而整体失败，没有部分写入。两个失败均已追加、未被后续命令掩盖，也不改变零候选科学结论。修订口径为 Campaign142 共 9 次尝试，其中 6 个值前概念、3 次基础设施失败；累计历史尝试 `1192`，收益读取开发试验仍为 `313`，完整定义/比较器仍为 `153/141`。严格复验通过前不把验证状态标为完成。

### 前序兼容性复验追加

第二次严格验证运行 10 项测试，9 项通过；唯一失败是 Campaign141 旧状态仍要求两个累积统一报告维持 Campaign141 时的文件哈希，而 Campaign142 的追加式章节已合法改变它们。Campaign141 终态、政策和其余 8 条不可变绑定全部通过。该退出码 1 已追加，验证器没有修改或放宽；兼容性断言仅改为精确接受这两个已知可变报告绑定失配并继续要求其余绑定通过。当前有效口径为 Campaign142 10 次尝试、4 次基础设施失败，累计历史尝试 `1193`，收益读取开发试验 `313`，库仍为 `153/141`。

### 严格复验通过

生命周期感知修正后，同一边界下的严格复验成功：21 个 Campaign142 JSON 文档全部可解析，Campaign141/142 两个测试文件均通过 Black 与 Ruff，聚焦兼容性测试 `10 passed`。没有修改绑定验证器、科学定义、门槛或台账，Campaign142 的零候选结论与 `1193 / 313 / 153/141` 最终口径保持不变；Candidate49 仍为空且未变。
